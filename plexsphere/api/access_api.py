"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


import warnings
from pydantic import validate_call, Field, StrictFloat, StrictStr, StrictInt
from typing import Any, Dict, List, Optional, Tuple, Union
from typing_extensions import Annotated

from pydantic import Field, StrictStr
from typing import Optional
from typing_extensions import Annotated
from uuid import UUID
from plexsphere.models.issue_session_request import IssueSessionRequest
from plexsphere.models.issued_session import IssuedSession
from plexsphere.models.revoke_session_request import RevokeSessionRequest
from plexsphere.models.session import Session
from plexsphere.models.session_activity_request import SessionActivityRequest
from plexsphere.models.session_kind import SessionKind
from plexsphere.models.session_list import SessionList
from plexsphere.models.session_status import SessionStatus

from plexsphere.api_client import ApiClient, RequestSerialized
from plexsphere.api_response import ApiResponse
from plexsphere.rest import RESTResponseType


class AccessApi:
    """NOTE: This class is auto generated by OpenAPI Generator
    Ref: https://openapi-generator.tech

    Do not edit the class manually.
    """

    def __init__(self, api_client=None) -> None:
        if api_client is None:
            api_client = ApiClient.get_default()
        self.api_client = api_client


    @validate_call
    def attach_session(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach. ")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> None:
        """Attach to a live SSH session as a bidirectional WebSocket stream.

        Upgrades the request to a WebSocket and bridges the browser to the mediated SSH session identified by `{session_id}` within the owning Project. The handler re-runs the issuance-equivalent ReBAC check on the session's Resource BEFORE dialing anything, validates the Session is `active` and `ssh`-kind, then dials the target plexd `listener_endpoint` over the mesh and copies bytes bidirectionally between the client socket and the mesh connection.  This operation does NOT speak JSON. On a successful upgrade the response is `101 Switching Protocols` and the connection body is an opaque, bidirectional byte stream carrying the SSH transport — the request and response bodies are NOT JSON documents. The `listener_endpoint` is never written to the client; the browser only ever sees the proxied stream. Error paths (before the upgrade completes) follow the RFC 9457 `application/problem+json` envelope. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param session_id: Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach.  (required)
        :type session_id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._attach_session_serialize(
            project_id=project_id,
            session_id=session_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '101': None,
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '426': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def attach_session_with_http_info(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach. ")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[None]:
        """Attach to a live SSH session as a bidirectional WebSocket stream.

        Upgrades the request to a WebSocket and bridges the browser to the mediated SSH session identified by `{session_id}` within the owning Project. The handler re-runs the issuance-equivalent ReBAC check on the session's Resource BEFORE dialing anything, validates the Session is `active` and `ssh`-kind, then dials the target plexd `listener_endpoint` over the mesh and copies bytes bidirectionally between the client socket and the mesh connection.  This operation does NOT speak JSON. On a successful upgrade the response is `101 Switching Protocols` and the connection body is an opaque, bidirectional byte stream carrying the SSH transport — the request and response bodies are NOT JSON documents. The `listener_endpoint` is never written to the client; the browser only ever sees the proxied stream. Error paths (before the upgrade completes) follow the RFC 9457 `application/problem+json` envelope. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param session_id: Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach.  (required)
        :type session_id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._attach_session_serialize(
            project_id=project_id,
            session_id=session_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '101': None,
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '426': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def attach_session_without_preload_content(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach. ")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Attach to a live SSH session as a bidirectional WebSocket stream.

        Upgrades the request to a WebSocket and bridges the browser to the mediated SSH session identified by `{session_id}` within the owning Project. The handler re-runs the issuance-equivalent ReBAC check on the session's Resource BEFORE dialing anything, validates the Session is `active` and `ssh`-kind, then dials the target plexd `listener_endpoint` over the mesh and copies bytes bidirectionally between the client socket and the mesh connection.  This operation does NOT speak JSON. On a successful upgrade the response is `101 Switching Protocols` and the connection body is an opaque, bidirectional byte stream carrying the SSH transport — the request and response bodies are NOT JSON documents. The `listener_endpoint` is never written to the client; the browser only ever sees the proxied stream. Error paths (before the upgrade completes) follow the RFC 9457 `application/problem+json` envelope. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param session_id: Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach.  (required)
        :type session_id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._attach_session_serialize(
            project_id=project_id,
            session_id=session_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '101': None,
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '426': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _attach_session_serialize(
        self,
        project_id,
        session_id,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if project_id is not None:
            _path_params['project_id'] = project_id
        if session_id is not None:
            _path_params['session_id'] = session_id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
            'operatorBearer'
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/v1/projects/{project_id}/sessions/{session_id}/attach',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def get_session(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach. ")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> Session:
        """Inspect a single mediated session.

        Returns the metadata-only projection of the Session identified by `{session_id}` within the owning Project. The handler runs the session-read ReBAC check BEFORE the persistence read. The projection never carries the signed token — the token is delivered only once, on issuance. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param session_id: Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach.  (required)
        :type session_id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_session_serialize(
            project_id=project_id,
            session_id=session_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "Session",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def get_session_with_http_info(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach. ")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[Session]:
        """Inspect a single mediated session.

        Returns the metadata-only projection of the Session identified by `{session_id}` within the owning Project. The handler runs the session-read ReBAC check BEFORE the persistence read. The projection never carries the signed token — the token is delivered only once, on issuance. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param session_id: Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach.  (required)
        :type session_id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_session_serialize(
            project_id=project_id,
            session_id=session_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "Session",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def get_session_without_preload_content(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach. ")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Inspect a single mediated session.

        Returns the metadata-only projection of the Session identified by `{session_id}` within the owning Project. The handler runs the session-read ReBAC check BEFORE the persistence read. The projection never carries the signed token — the token is delivered only once, on issuance. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param session_id: Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach.  (required)
        :type session_id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_session_serialize(
            project_id=project_id,
            session_id=session_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "Session",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _get_session_serialize(
        self,
        project_id,
        session_id,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if project_id is not None:
            _path_params['project_id'] = project_id
        if session_id is not None:
            _path_params['session_id'] = session_id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
            'operatorBearer'
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/v1/projects/{project_id}/sessions/{session_id}',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def issue_session(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        issue_session_request: IssueSessionRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> IssuedSession:
        """Issue a short-lived, session-scoped JWT for a mediated target.

        Issues a short-lived, session-scoped EdDSA JWT for one mediated target inside the owning Project. The handler runs the issuance ReBAC check on the targeted Resource BEFORE any persistence write, enforces the per-Domain step-up policy, clamps the requested TTL to the per-Domain ceiling, admits the session against the per-Domain concurrency and rate budgets, then mints one Session whose JTI is its own identifier and signs the canonical claim bytes with the per-Domain Signing Service key.  The request body supplies the targeted `resource_id`, the session `kind` (`ssh`, `k8s`, or `tcp`), the matching per-kind `target`, an optional `requested_ttl_seconds` (silently clamped to the per-Domain maximum), and an optional `idempotency_key` (a repeat within the dedupe window returns the original session and token).  The signed token is returned EXACTLY ONCE, in this response. It is never persisted server-side beyond its identifier and expiry, and the list and read projections never re-expose it. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param issue_session_request: (required)
        :type issue_session_request: IssueSessionRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._issue_session_serialize(
            project_id=project_id,
            issue_session_request=issue_session_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '201': "IssuedSession",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '503': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def issue_session_with_http_info(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        issue_session_request: IssueSessionRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[IssuedSession]:
        """Issue a short-lived, session-scoped JWT for a mediated target.

        Issues a short-lived, session-scoped EdDSA JWT for one mediated target inside the owning Project. The handler runs the issuance ReBAC check on the targeted Resource BEFORE any persistence write, enforces the per-Domain step-up policy, clamps the requested TTL to the per-Domain ceiling, admits the session against the per-Domain concurrency and rate budgets, then mints one Session whose JTI is its own identifier and signs the canonical claim bytes with the per-Domain Signing Service key.  The request body supplies the targeted `resource_id`, the session `kind` (`ssh`, `k8s`, or `tcp`), the matching per-kind `target`, an optional `requested_ttl_seconds` (silently clamped to the per-Domain maximum), and an optional `idempotency_key` (a repeat within the dedupe window returns the original session and token).  The signed token is returned EXACTLY ONCE, in this response. It is never persisted server-side beyond its identifier and expiry, and the list and read projections never re-expose it. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param issue_session_request: (required)
        :type issue_session_request: IssueSessionRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._issue_session_serialize(
            project_id=project_id,
            issue_session_request=issue_session_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '201': "IssuedSession",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '503': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def issue_session_without_preload_content(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        issue_session_request: IssueSessionRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Issue a short-lived, session-scoped JWT for a mediated target.

        Issues a short-lived, session-scoped EdDSA JWT for one mediated target inside the owning Project. The handler runs the issuance ReBAC check on the targeted Resource BEFORE any persistence write, enforces the per-Domain step-up policy, clamps the requested TTL to the per-Domain ceiling, admits the session against the per-Domain concurrency and rate budgets, then mints one Session whose JTI is its own identifier and signs the canonical claim bytes with the per-Domain Signing Service key.  The request body supplies the targeted `resource_id`, the session `kind` (`ssh`, `k8s`, or `tcp`), the matching per-kind `target`, an optional `requested_ttl_seconds` (silently clamped to the per-Domain maximum), and an optional `idempotency_key` (a repeat within the dedupe window returns the original session and token).  The signed token is returned EXACTLY ONCE, in this response. It is never persisted server-side beyond its identifier and expiry, and the list and read projections never re-expose it. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param issue_session_request: (required)
        :type issue_session_request: IssueSessionRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._issue_session_serialize(
            project_id=project_id,
            issue_session_request=issue_session_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '201': "IssuedSession",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '503': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _issue_session_serialize(
        self,
        project_id,
        issue_session_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if project_id is not None:
            _path_params['project_id'] = project_id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter
        if issue_session_request is not None:
            _body_params = issue_session_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
            'operatorBearer'
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/projects/{project_id}/sessions',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def list_sessions(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        status: Annotated[Optional[SessionStatus], Field(description="Optional aggregate-status filter. When present, only Sessions whose status equals the named value are returned. ")] = None,
        kind: Annotated[Optional[SessionKind], Field(description="Optional kind filter. When present, only Sessions of the named kind are returned. ")] = None,
        identity_id: Annotated[Optional[UUID], Field(description="Optional identity filter. When present, only Sessions issued to the named Identity are returned. ")] = None,
        cursor: Annotated[Optional[StrictStr], Field(description="Opaque continuation token returned by a previous call's `next_cursor`. The encoding is HMAC-signed by the server so a tampered cursor surfaces as `400`. ")] = None,
        limit: Annotated[Optional[Annotated[int, Field(le=200, strict=True, ge=1)]], Field(description="Maximum number of items to return in a single page. The handler clamps the value to [1, 200] before forwarding it to the read service. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> SessionList:
        """List mediated sessions for a Project.

        Returns an issuance-ordered page of Session lifecycle metadata for the owning Project, optionally narrowed by aggregate `status`, `kind`, or `identity_id`. The handler runs the session-read ReBAC check BEFORE the persistence read. The projection is metadata-only — it carries the session identity, the aggregate status, and the per-kind target, but never the signed token.  The pagination cursor is opaque and HMAC-signed by the server, so a tampered cursor surfaces as `400` on the next call. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param status: Optional aggregate-status filter. When present, only Sessions whose status equals the named value are returned. 
        :type status: SessionStatus
        :param kind: Optional kind filter. When present, only Sessions of the named kind are returned. 
        :type kind: SessionKind
        :param identity_id: Optional identity filter. When present, only Sessions issued to the named Identity are returned. 
        :type identity_id: UUID
        :param cursor: Opaque continuation token returned by a previous call's `next_cursor`. The encoding is HMAC-signed by the server so a tampered cursor surfaces as `400`. 
        :type cursor: str
        :param limit: Maximum number of items to return in a single page. The handler clamps the value to [1, 200] before forwarding it to the read service. 
        :type limit: int
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._list_sessions_serialize(
            project_id=project_id,
            status=status,
            kind=kind,
            identity_id=identity_id,
            cursor=cursor,
            limit=limit,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "SessionList",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def list_sessions_with_http_info(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        status: Annotated[Optional[SessionStatus], Field(description="Optional aggregate-status filter. When present, only Sessions whose status equals the named value are returned. ")] = None,
        kind: Annotated[Optional[SessionKind], Field(description="Optional kind filter. When present, only Sessions of the named kind are returned. ")] = None,
        identity_id: Annotated[Optional[UUID], Field(description="Optional identity filter. When present, only Sessions issued to the named Identity are returned. ")] = None,
        cursor: Annotated[Optional[StrictStr], Field(description="Opaque continuation token returned by a previous call's `next_cursor`. The encoding is HMAC-signed by the server so a tampered cursor surfaces as `400`. ")] = None,
        limit: Annotated[Optional[Annotated[int, Field(le=200, strict=True, ge=1)]], Field(description="Maximum number of items to return in a single page. The handler clamps the value to [1, 200] before forwarding it to the read service. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[SessionList]:
        """List mediated sessions for a Project.

        Returns an issuance-ordered page of Session lifecycle metadata for the owning Project, optionally narrowed by aggregate `status`, `kind`, or `identity_id`. The handler runs the session-read ReBAC check BEFORE the persistence read. The projection is metadata-only — it carries the session identity, the aggregate status, and the per-kind target, but never the signed token.  The pagination cursor is opaque and HMAC-signed by the server, so a tampered cursor surfaces as `400` on the next call. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param status: Optional aggregate-status filter. When present, only Sessions whose status equals the named value are returned. 
        :type status: SessionStatus
        :param kind: Optional kind filter. When present, only Sessions of the named kind are returned. 
        :type kind: SessionKind
        :param identity_id: Optional identity filter. When present, only Sessions issued to the named Identity are returned. 
        :type identity_id: UUID
        :param cursor: Opaque continuation token returned by a previous call's `next_cursor`. The encoding is HMAC-signed by the server so a tampered cursor surfaces as `400`. 
        :type cursor: str
        :param limit: Maximum number of items to return in a single page. The handler clamps the value to [1, 200] before forwarding it to the read service. 
        :type limit: int
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._list_sessions_serialize(
            project_id=project_id,
            status=status,
            kind=kind,
            identity_id=identity_id,
            cursor=cursor,
            limit=limit,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "SessionList",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def list_sessions_without_preload_content(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        status: Annotated[Optional[SessionStatus], Field(description="Optional aggregate-status filter. When present, only Sessions whose status equals the named value are returned. ")] = None,
        kind: Annotated[Optional[SessionKind], Field(description="Optional kind filter. When present, only Sessions of the named kind are returned. ")] = None,
        identity_id: Annotated[Optional[UUID], Field(description="Optional identity filter. When present, only Sessions issued to the named Identity are returned. ")] = None,
        cursor: Annotated[Optional[StrictStr], Field(description="Opaque continuation token returned by a previous call's `next_cursor`. The encoding is HMAC-signed by the server so a tampered cursor surfaces as `400`. ")] = None,
        limit: Annotated[Optional[Annotated[int, Field(le=200, strict=True, ge=1)]], Field(description="Maximum number of items to return in a single page. The handler clamps the value to [1, 200] before forwarding it to the read service. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """List mediated sessions for a Project.

        Returns an issuance-ordered page of Session lifecycle metadata for the owning Project, optionally narrowed by aggregate `status`, `kind`, or `identity_id`. The handler runs the session-read ReBAC check BEFORE the persistence read. The projection is metadata-only — it carries the session identity, the aggregate status, and the per-kind target, but never the signed token.  The pagination cursor is opaque and HMAC-signed by the server, so a tampered cursor surfaces as `400` on the next call. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param status: Optional aggregate-status filter. When present, only Sessions whose status equals the named value are returned. 
        :type status: SessionStatus
        :param kind: Optional kind filter. When present, only Sessions of the named kind are returned. 
        :type kind: SessionKind
        :param identity_id: Optional identity filter. When present, only Sessions issued to the named Identity are returned. 
        :type identity_id: UUID
        :param cursor: Opaque continuation token returned by a previous call's `next_cursor`. The encoding is HMAC-signed by the server so a tampered cursor surfaces as `400`. 
        :type cursor: str
        :param limit: Maximum number of items to return in a single page. The handler clamps the value to [1, 200] before forwarding it to the read service. 
        :type limit: int
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._list_sessions_serialize(
            project_id=project_id,
            status=status,
            kind=kind,
            identity_id=identity_id,
            cursor=cursor,
            limit=limit,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "SessionList",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _list_sessions_serialize(
        self,
        project_id,
        status,
        kind,
        identity_id,
        cursor,
        limit,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if project_id is not None:
            _path_params['project_id'] = project_id
        # process the query parameters
        if status is not None:
            
            _query_params.append(('status', status.value))
            
        if kind is not None:
            
            _query_params.append(('kind', kind.value))
            
        if identity_id is not None:
            
            _query_params.append(('identity_id', identity_id))
            
        if cursor is not None:
            
            _query_params.append(('cursor', cursor))
            
        if limit is not None:
            
            _query_params.append(('limit', limit))
            
        # process the header parameters
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
            'operatorBearer'
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/v1/projects/{project_id}/sessions',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def post_node_session_callback(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the callback scope.")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7) the activity belongs to.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `nsk_node_mismatch`. ")],
        session_activity_request: SessionActivityRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> None:
        """Report per-kind session activity from a Node.

        Accepts a per-kind session-activity callback from the target plexd and appends it to the session's append-only activity log, touching the session's last-active timestamp so the idle-timeout sweeper sees progress. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      rejecting revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `nsk_node_mismatch` so a leaked NSK cannot be replayed      against a sibling Node's session.   3. Appends the reported per-kind activity row. An `ssh` session      records `command_executed` / `command_exited` activity; a      `k8s` session records `api_request` activity; a `tcp` session      records exactly a `session_started` and a `session_ended`      pair — the TCP shape is deliberately payload-blind, with no      L7 parsing.  DEFERRED-WIRING POSTURE: until the production composition root supplies the callback service and its NSK validator, every request to this endpoint returns 501 with `code: access_session_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the callback scope. (required)
        :type id: UUID
        :param session_id: Session identifier (UUIDv7) the activity belongs to. (required)
        :type session_id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `nsk_node_mismatch`.  (required)
        :type authorization: str
        :param session_activity_request: (required)
        :type session_activity_request: SessionActivityRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_session_callback_serialize(
            id=id,
            session_id=session_id,
            authorization=authorization,
            session_activity_request=session_activity_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '204': None,
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def post_node_session_callback_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the callback scope.")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7) the activity belongs to.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `nsk_node_mismatch`. ")],
        session_activity_request: SessionActivityRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[None]:
        """Report per-kind session activity from a Node.

        Accepts a per-kind session-activity callback from the target plexd and appends it to the session's append-only activity log, touching the session's last-active timestamp so the idle-timeout sweeper sees progress. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      rejecting revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `nsk_node_mismatch` so a leaked NSK cannot be replayed      against a sibling Node's session.   3. Appends the reported per-kind activity row. An `ssh` session      records `command_executed` / `command_exited` activity; a      `k8s` session records `api_request` activity; a `tcp` session      records exactly a `session_started` and a `session_ended`      pair — the TCP shape is deliberately payload-blind, with no      L7 parsing.  DEFERRED-WIRING POSTURE: until the production composition root supplies the callback service and its NSK validator, every request to this endpoint returns 501 with `code: access_session_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the callback scope. (required)
        :type id: UUID
        :param session_id: Session identifier (UUIDv7) the activity belongs to. (required)
        :type session_id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `nsk_node_mismatch`.  (required)
        :type authorization: str
        :param session_activity_request: (required)
        :type session_activity_request: SessionActivityRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_session_callback_serialize(
            id=id,
            session_id=session_id,
            authorization=authorization,
            session_activity_request=session_activity_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '204': None,
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def post_node_session_callback_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the callback scope.")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7) the activity belongs to.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `nsk_node_mismatch`. ")],
        session_activity_request: SessionActivityRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Report per-kind session activity from a Node.

        Accepts a per-kind session-activity callback from the target plexd and appends it to the session's append-only activity log, touching the session's last-active timestamp so the idle-timeout sweeper sees progress. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      rejecting revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `nsk_node_mismatch` so a leaked NSK cannot be replayed      against a sibling Node's session.   3. Appends the reported per-kind activity row. An `ssh` session      records `command_executed` / `command_exited` activity; a      `k8s` session records `api_request` activity; a `tcp` session      records exactly a `session_started` and a `session_ended`      pair — the TCP shape is deliberately payload-blind, with no      L7 parsing.  DEFERRED-WIRING POSTURE: until the production composition root supplies the callback service and its NSK validator, every request to this endpoint returns 501 with `code: access_session_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the callback scope. (required)
        :type id: UUID
        :param session_id: Session identifier (UUIDv7) the activity belongs to. (required)
        :type session_id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `nsk_node_mismatch`.  (required)
        :type authorization: str
        :param session_activity_request: (required)
        :type session_activity_request: SessionActivityRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_session_callback_serialize(
            id=id,
            session_id=session_id,
            authorization=authorization,
            session_activity_request=session_activity_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '204': None,
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _post_node_session_callback_serialize(
        self,
        id,
        session_id,
        authorization,
        session_activity_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        if session_id is not None:
            _path_params['session_id'] = session_id
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        # process the form parameters
        # process the body parameter
        if session_activity_request is not None:
            _body_params = session_activity_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/nodes/{id}/sessions/{session_id}',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def revoke_session(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach. ")],
        revoke_session_request: RevokeSessionRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> None:
        """Instantly revoke a live mediated session.

        Revokes the Session identified by `{session_id}` within the owning Project. The handler runs the revoke ReBAC check BEFORE any write, then writes the revocation, appends the session identifier to the revocation denylist, and fans a revocation event out over the Signed Event Bus so the target plexd tears the listener down — all in one transaction.  Re-revoking an already-revoked Session is idempotent: it returns `204` and writes no additional rows. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param session_id: Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach.  (required)
        :type session_id: UUID
        :param revoke_session_request: (required)
        :type revoke_session_request: RevokeSessionRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._revoke_session_serialize(
            project_id=project_id,
            session_id=session_id,
            revoke_session_request=revoke_session_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '204': None,
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def revoke_session_with_http_info(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach. ")],
        revoke_session_request: RevokeSessionRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[None]:
        """Instantly revoke a live mediated session.

        Revokes the Session identified by `{session_id}` within the owning Project. The handler runs the revoke ReBAC check BEFORE any write, then writes the revocation, appends the session identifier to the revocation denylist, and fans a revocation event out over the Signed Event Bus so the target plexd tears the listener down — all in one transaction.  Re-revoking an already-revoked Session is idempotent: it returns `204` and writes no additional rows. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param session_id: Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach.  (required)
        :type session_id: UUID
        :param revoke_session_request: (required)
        :type revoke_session_request: RevokeSessionRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._revoke_session_serialize(
            project_id=project_id,
            session_id=session_id,
            revoke_session_request=revoke_session_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '204': None,
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def revoke_session_without_preload_content(
        self,
        project_id: Annotated[UUID, Field(description="Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path. ")],
        session_id: Annotated[UUID, Field(description="Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach. ")],
        revoke_session_request: RevokeSessionRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Instantly revoke a live mediated session.

        Revokes the Session identified by `{session_id}` within the owning Project. The handler runs the revoke ReBAC check BEFORE any write, then writes the revocation, appends the session identifier to the revocation denylist, and fans a revocation event out over the Signed Event Bus so the target plexd tears the listener down — all in one transaction.  Re-revoking an already-revoked Session is idempotent: it returns `204` and writes no additional rows. 

        :param project_id: Owning Project. The Access Orchestrator issue, list, read, and revoke operations are scoped per Project, so every operator-facing endpoint requires the Project identifier in the path.  (required)
        :type project_id: UUID
        :param session_id: Session identifier (UUIDv7). Bound on `/v1/projects/{project_id}/sessions/{session_id}`, its `:revoke` sub-resource, and its `/attach` WebSocket gateway for the single-Session read, revoke, and attach.  (required)
        :type session_id: UUID
        :param revoke_session_request: (required)
        :type revoke_session_request: RevokeSessionRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._revoke_session_serialize(
            project_id=project_id,
            session_id=session_id,
            revoke_session_request=revoke_session_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '204': None,
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _revoke_session_serialize(
        self,
        project_id,
        session_id,
        revoke_session_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if project_id is not None:
            _path_params['project_id'] = project_id
        if session_id is not None:
            _path_params['session_id'] = session_id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter
        if revoke_session_request is not None:
            _body_params = revoke_session_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
            'operatorBearer'
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/projects/{project_id}/sessions/{session_id}:revoke',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )


