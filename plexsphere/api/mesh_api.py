"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


import warnings
from pydantic import validate_call, Field, StrictFloat, StrictStr, StrictInt
from typing import Any, Dict, List, Optional, Tuple, Union
from typing_extensions import Annotated

from datetime import datetime
from pydantic import Field, StrictBytes, StrictStr, field_validator
from typing import List, Optional, Tuple, Union
from typing_extensions import Annotated
from uuid import UUID
from plexsphere.models.audit_event import AuditEvent
from plexsphere.models.capability_manifest_request import CapabilityManifestRequest
from plexsphere.models.capability_manifest_response import CapabilityManifestResponse
from plexsphere.models.endpoint_request import EndpointRequest
from plexsphere.models.endpoint_response import EndpointResponse
from plexsphere.models.heartbeat_request import HeartbeatRequest
from plexsphere.models.heartbeat_response import HeartbeatResponse
from plexsphere.models.ingest_receipt import IngestReceipt
from plexsphere.models.integrity_violations_request import IntegrityViolationsRequest
from plexsphere.models.integrity_violations_response import IntegrityViolationsResponse
from plexsphere.models.keys_rotate_request import KeysRotateRequest
from plexsphere.models.keys_rotate_response import KeysRotateResponse
from plexsphere.models.log_line import LogLine
from plexsphere.models.mesh_topology import MeshTopology
from plexsphere.models.metric_sample import MetricSample
from plexsphere.models.node_state_report_request import NodeStateReportRequest
from plexsphere.models.node_state_report_response import NodeStateReportResponse
from plexsphere.models.node_state_snapshot import NodeStateSnapshot
from plexsphere.models.reachability import Reachability
from plexsphere.models.rotation_impact_preview import RotationImpactPreview
from plexsphere.models.rotation_trigger_response import RotationTriggerResponse

from plexsphere.api_client import ApiClient, RequestSerialized
from plexsphere.api_response import ApiResponse
from plexsphere.rest import RESTResponseType


class MeshApi:
    """NOTE: This class is auto generated by OpenAPI Generator
    Ref: https://openapi-generator.tech

    Do not edit the class manually.
    """

    def __init__(self, api_client=None) -> None:
        if api_client is None:
            api_client = ApiClient.get_default()
        self.api_client = api_client


    @validate_call
    def delete_node_state_report(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the report-delete scope.")],
        key: Annotated[str, Field(strict=True, description="Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters). ")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key. Only a Node proving possession of its NSK may delete a report; a missing, malformed, or revoked credential surfaces as 401. ")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> None:
        """Delete an upstream node-state report entry for a Node.

        Deletes the upstream node-state report entry the addressed Node recorded under the addressed key. Deleting a report emits NO `node_state_updated` event. A delete for a key the Node carries no report under is answered with 404.  The caller authenticates with its NSK in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only delete its own reports.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`. 

        :param id: Node identifier (UUIDv7) — the report-delete scope. (required)
        :type id: UUID
        :param key: Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).  (required)
        :type key: str
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key. Only a Node proving possession of its NSK may delete a report; a missing, malformed, or revoked credential surfaces as 401.  (required)
        :type authorization: str
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._delete_node_state_report_serialize(
            id=id,
            key=key,
            authorization=authorization,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '204': None,
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def delete_node_state_report_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the report-delete scope.")],
        key: Annotated[str, Field(strict=True, description="Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters). ")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key. Only a Node proving possession of its NSK may delete a report; a missing, malformed, or revoked credential surfaces as 401. ")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[None]:
        """Delete an upstream node-state report entry for a Node.

        Deletes the upstream node-state report entry the addressed Node recorded under the addressed key. Deleting a report emits NO `node_state_updated` event. A delete for a key the Node carries no report under is answered with 404.  The caller authenticates with its NSK in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only delete its own reports.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`. 

        :param id: Node identifier (UUIDv7) — the report-delete scope. (required)
        :type id: UUID
        :param key: Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).  (required)
        :type key: str
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key. Only a Node proving possession of its NSK may delete a report; a missing, malformed, or revoked credential surfaces as 401.  (required)
        :type authorization: str
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._delete_node_state_report_serialize(
            id=id,
            key=key,
            authorization=authorization,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '204': None,
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def delete_node_state_report_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the report-delete scope.")],
        key: Annotated[str, Field(strict=True, description="Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters). ")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key. Only a Node proving possession of its NSK may delete a report; a missing, malformed, or revoked credential surfaces as 401. ")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Delete an upstream node-state report entry for a Node.

        Deletes the upstream node-state report entry the addressed Node recorded under the addressed key. Deleting a report emits NO `node_state_updated` event. A delete for a key the Node carries no report under is answered with 404.  The caller authenticates with its NSK in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only delete its own reports.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`. 

        :param id: Node identifier (UUIDv7) — the report-delete scope. (required)
        :type id: UUID
        :param key: Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).  (required)
        :type key: str
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key. Only a Node proving possession of its NSK may delete a report; a missing, malformed, or revoked credential surfaces as 401.  (required)
        :type authorization: str
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._delete_node_state_report_serialize(
            id=id,
            key=key,
            authorization=authorization,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '204': None,
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _delete_node_state_report_serialize(
        self,
        id,
        key,
        authorization,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        if key is not None:
            _path_params['key'] = key
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='DELETE',
            resource_path='/v1/nodes/{id}/state/reports/{key}',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def fetch_node_secret(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the secret-fetch scope.")],
        name: Annotated[str, Field(strict=True, description="Secret name within the owning Project. Lower-case, starts with a letter, and is limited to letters, digits, hyphen, and underscore (1 to 63 characters). ")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may fetch a secret; a missing, malformed, or revoked credential surfaces as 401. ")],
        version: Annotated[Optional[Annotated[int, Field(strict=True, ge=1)]], Field(description="Optional OpenBao KV v2 version to fetch. When omitted the current version is served. The version actually served is always reported in the `X-Plexsphere-Secret-Version` response header. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> bytes:
        """Fetch a Secret Store entry rewrapped under the calling Node's NSK.

        Returns the named Secret Store entry for the addressed Node as an AES-256-GCM ciphertext envelope rewrapped under the calling Node's Node Secret Key (NSK). The Secret Store is the platform's only meet-and-rewrap point between the OpenBao backend and the per-Node NSK: the handler reads the OpenBao plaintext, recovers the NSK, and rewraps the payload in-process so that ONLY NSK-wrapped ciphertext ever crosses the wire. The transient OpenBao plaintext and the recovered NSK are zeroed on every exit path; no plaintext is ever persisted, logged, cached, or placed on the event bus.  The 200 body is the raw envelope `<12-byte nonce> || <ciphertext + 16-byte GCM tag>`, served as `application/octet-stream`. The caller recovers the seeded plaintext byte-for-byte with `AES-256-GCM-Open` under its NSK. The response carries the served version in `X-Plexsphere-Secret-Version`, the NSK key id used for the wrap in `X-Plexsphere-Secret-KID` (so plexd can pick the right NSK during a rotation overlap), and `Cache-Control: no-store` so no intermediary ever retains the ciphertext.  The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401. Only a      Node proving possession of its NSK can reach the rewrap      pipeline.   2. Resolves the addressed secret metadata, applies the      per-Node and per-Domain rate limits, and runs the      `node-agent` ReBAC visibility check on the addressed Node.      An authenticated caller without the relation receives 403      `PermissionDenied`; the denial is recorded with      `insufficient_relation`.   3. Reads the entry from the OpenBao backend — the current      version when `version` is omitted, or the requested version      when present — reconciling the served version against the      metadata; the OpenBao-returned version wins for the header.   4. Rewraps the payload under the recovered NSK with a fresh      12-byte nonce and emits exactly one granted audit row. A      backend read that fails AFTER a granted authorization still      records the granted audit row — operational outcomes are      carried on the fetch-duration metric, never on the audit      chain.  The response body is capped at 1 MiB of ciphertext.  DEFERRED-WIRING POSTURE: when the Secret Store backend is not configured (empty backend DSN) the production composition root leaves this surface disabled and every request returns 501 with `code: secrets_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the secret-fetch scope. (required)
        :type id: UUID
        :param name: Secret name within the owning Project. Lower-case, starts with a letter, and is limited to letters, digits, hyphen, and underscore (1 to 63 characters).  (required)
        :type name: str
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may fetch a secret; a missing, malformed, or revoked credential surfaces as 401.  (required)
        :type authorization: str
        :param version: Optional OpenBao KV v2 version to fetch. When omitted the current version is served. The version actually served is always reported in the `X-Plexsphere-Secret-Version` response header. 
        :type version: int
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._fetch_node_secret_serialize(
            id=id,
            name=name,
            authorization=authorization,
            version=version,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "bytes",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '429': "Problem",
            '503': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def fetch_node_secret_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the secret-fetch scope.")],
        name: Annotated[str, Field(strict=True, description="Secret name within the owning Project. Lower-case, starts with a letter, and is limited to letters, digits, hyphen, and underscore (1 to 63 characters). ")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may fetch a secret; a missing, malformed, or revoked credential surfaces as 401. ")],
        version: Annotated[Optional[Annotated[int, Field(strict=True, ge=1)]], Field(description="Optional OpenBao KV v2 version to fetch. When omitted the current version is served. The version actually served is always reported in the `X-Plexsphere-Secret-Version` response header. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[bytes]:
        """Fetch a Secret Store entry rewrapped under the calling Node's NSK.

        Returns the named Secret Store entry for the addressed Node as an AES-256-GCM ciphertext envelope rewrapped under the calling Node's Node Secret Key (NSK). The Secret Store is the platform's only meet-and-rewrap point between the OpenBao backend and the per-Node NSK: the handler reads the OpenBao plaintext, recovers the NSK, and rewraps the payload in-process so that ONLY NSK-wrapped ciphertext ever crosses the wire. The transient OpenBao plaintext and the recovered NSK are zeroed on every exit path; no plaintext is ever persisted, logged, cached, or placed on the event bus.  The 200 body is the raw envelope `<12-byte nonce> || <ciphertext + 16-byte GCM tag>`, served as `application/octet-stream`. The caller recovers the seeded plaintext byte-for-byte with `AES-256-GCM-Open` under its NSK. The response carries the served version in `X-Plexsphere-Secret-Version`, the NSK key id used for the wrap in `X-Plexsphere-Secret-KID` (so plexd can pick the right NSK during a rotation overlap), and `Cache-Control: no-store` so no intermediary ever retains the ciphertext.  The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401. Only a      Node proving possession of its NSK can reach the rewrap      pipeline.   2. Resolves the addressed secret metadata, applies the      per-Node and per-Domain rate limits, and runs the      `node-agent` ReBAC visibility check on the addressed Node.      An authenticated caller without the relation receives 403      `PermissionDenied`; the denial is recorded with      `insufficient_relation`.   3. Reads the entry from the OpenBao backend — the current      version when `version` is omitted, or the requested version      when present — reconciling the served version against the      metadata; the OpenBao-returned version wins for the header.   4. Rewraps the payload under the recovered NSK with a fresh      12-byte nonce and emits exactly one granted audit row. A      backend read that fails AFTER a granted authorization still      records the granted audit row — operational outcomes are      carried on the fetch-duration metric, never on the audit      chain.  The response body is capped at 1 MiB of ciphertext.  DEFERRED-WIRING POSTURE: when the Secret Store backend is not configured (empty backend DSN) the production composition root leaves this surface disabled and every request returns 501 with `code: secrets_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the secret-fetch scope. (required)
        :type id: UUID
        :param name: Secret name within the owning Project. Lower-case, starts with a letter, and is limited to letters, digits, hyphen, and underscore (1 to 63 characters).  (required)
        :type name: str
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may fetch a secret; a missing, malformed, or revoked credential surfaces as 401.  (required)
        :type authorization: str
        :param version: Optional OpenBao KV v2 version to fetch. When omitted the current version is served. The version actually served is always reported in the `X-Plexsphere-Secret-Version` response header. 
        :type version: int
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._fetch_node_secret_serialize(
            id=id,
            name=name,
            authorization=authorization,
            version=version,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "bytes",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '429': "Problem",
            '503': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def fetch_node_secret_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the secret-fetch scope.")],
        name: Annotated[str, Field(strict=True, description="Secret name within the owning Project. Lower-case, starts with a letter, and is limited to letters, digits, hyphen, and underscore (1 to 63 characters). ")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may fetch a secret; a missing, malformed, or revoked credential surfaces as 401. ")],
        version: Annotated[Optional[Annotated[int, Field(strict=True, ge=1)]], Field(description="Optional OpenBao KV v2 version to fetch. When omitted the current version is served. The version actually served is always reported in the `X-Plexsphere-Secret-Version` response header. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Fetch a Secret Store entry rewrapped under the calling Node's NSK.

        Returns the named Secret Store entry for the addressed Node as an AES-256-GCM ciphertext envelope rewrapped under the calling Node's Node Secret Key (NSK). The Secret Store is the platform's only meet-and-rewrap point between the OpenBao backend and the per-Node NSK: the handler reads the OpenBao plaintext, recovers the NSK, and rewraps the payload in-process so that ONLY NSK-wrapped ciphertext ever crosses the wire. The transient OpenBao plaintext and the recovered NSK are zeroed on every exit path; no plaintext is ever persisted, logged, cached, or placed on the event bus.  The 200 body is the raw envelope `<12-byte nonce> || <ciphertext + 16-byte GCM tag>`, served as `application/octet-stream`. The caller recovers the seeded plaintext byte-for-byte with `AES-256-GCM-Open` under its NSK. The response carries the served version in `X-Plexsphere-Secret-Version`, the NSK key id used for the wrap in `X-Plexsphere-Secret-KID` (so plexd can pick the right NSK during a rotation overlap), and `Cache-Control: no-store` so no intermediary ever retains the ciphertext.  The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401. Only a      Node proving possession of its NSK can reach the rewrap      pipeline.   2. Resolves the addressed secret metadata, applies the      per-Node and per-Domain rate limits, and runs the      `node-agent` ReBAC visibility check on the addressed Node.      An authenticated caller without the relation receives 403      `PermissionDenied`; the denial is recorded with      `insufficient_relation`.   3. Reads the entry from the OpenBao backend — the current      version when `version` is omitted, or the requested version      when present — reconciling the served version against the      metadata; the OpenBao-returned version wins for the header.   4. Rewraps the payload under the recovered NSK with a fresh      12-byte nonce and emits exactly one granted audit row. A      backend read that fails AFTER a granted authorization still      records the granted audit row — operational outcomes are      carried on the fetch-duration metric, never on the audit      chain.  The response body is capped at 1 MiB of ciphertext.  DEFERRED-WIRING POSTURE: when the Secret Store backend is not configured (empty backend DSN) the production composition root leaves this surface disabled and every request returns 501 with `code: secrets_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the secret-fetch scope. (required)
        :type id: UUID
        :param name: Secret name within the owning Project. Lower-case, starts with a letter, and is limited to letters, digits, hyphen, and underscore (1 to 63 characters).  (required)
        :type name: str
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may fetch a secret; a missing, malformed, or revoked credential surfaces as 401.  (required)
        :type authorization: str
        :param version: Optional OpenBao KV v2 version to fetch. When omitted the current version is served. The version actually served is always reported in the `X-Plexsphere-Secret-Version` response header. 
        :type version: int
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._fetch_node_secret_serialize(
            id=id,
            name=name,
            authorization=authorization,
            version=version,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "bytes",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '429': "Problem",
            '503': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _fetch_node_secret_serialize(
        self,
        id,
        name,
        authorization,
        version,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        if name is not None:
            _path_params['name'] = name
        # process the query parameters
        if version is not None:
            
            _query_params.append(('version', version))
            
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/octet-stream', 
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/v1/nodes/{id}/secrets/{name}',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def get_domain_mesh_topology(
        self,
        domain_id: Annotated[UUID, Field(description="Domain identifier (UUIDv7) — the topology scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> MeshTopology:
        """Return the mesh topology for a Domain.

        Returns the read-side projection of the mesh fabric that backs the dashboard mesh-map view and the `plexctl mesh topology` CLI: every live Peer Node anchored to the Domain together with the directed edges between them. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the `domain-view` ReBAC relation on the addressed      Domain BEFORE any existence check so the endpoint cannot      be used as a Domain-id oracle.   3. Resolves the per-Domain peer graph from the SSE peer-      delta projection — `nodes` carries each anchored Node's      mesh-IP and reachability, `edges` carries each pairwise      relationship with its `mode` (`direct` or `relayed`), the      handshake age, and the bridge Node currently serving as      the relay fallback when one is assigned.  The payload mirrors the SSE peer-graph events one-for-one so a renderer that consumes the live stream and a renderer that polls this pull converge on byte-identical state.  DEFERRED-WIRING POSTURE: until the production composition root supplies the mesh-topology projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: mesh_topology_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param domain_id: Domain identifier (UUIDv7) — the topology scope. (required)
        :type domain_id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_domain_mesh_topology_serialize(
            domain_id=domain_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "MeshTopology",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def get_domain_mesh_topology_with_http_info(
        self,
        domain_id: Annotated[UUID, Field(description="Domain identifier (UUIDv7) — the topology scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[MeshTopology]:
        """Return the mesh topology for a Domain.

        Returns the read-side projection of the mesh fabric that backs the dashboard mesh-map view and the `plexctl mesh topology` CLI: every live Peer Node anchored to the Domain together with the directed edges between them. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the `domain-view` ReBAC relation on the addressed      Domain BEFORE any existence check so the endpoint cannot      be used as a Domain-id oracle.   3. Resolves the per-Domain peer graph from the SSE peer-      delta projection — `nodes` carries each anchored Node's      mesh-IP and reachability, `edges` carries each pairwise      relationship with its `mode` (`direct` or `relayed`), the      handshake age, and the bridge Node currently serving as      the relay fallback when one is assigned.  The payload mirrors the SSE peer-graph events one-for-one so a renderer that consumes the live stream and a renderer that polls this pull converge on byte-identical state.  DEFERRED-WIRING POSTURE: until the production composition root supplies the mesh-topology projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: mesh_topology_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param domain_id: Domain identifier (UUIDv7) — the topology scope. (required)
        :type domain_id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_domain_mesh_topology_serialize(
            domain_id=domain_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "MeshTopology",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def get_domain_mesh_topology_without_preload_content(
        self,
        domain_id: Annotated[UUID, Field(description="Domain identifier (UUIDv7) — the topology scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Return the mesh topology for a Domain.

        Returns the read-side projection of the mesh fabric that backs the dashboard mesh-map view and the `plexctl mesh topology` CLI: every live Peer Node anchored to the Domain together with the directed edges between them. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the `domain-view` ReBAC relation on the addressed      Domain BEFORE any existence check so the endpoint cannot      be used as a Domain-id oracle.   3. Resolves the per-Domain peer graph from the SSE peer-      delta projection — `nodes` carries each anchored Node's      mesh-IP and reachability, `edges` carries each pairwise      relationship with its `mode` (`direct` or `relayed`), the      handshake age, and the bridge Node currently serving as      the relay fallback when one is assigned.  The payload mirrors the SSE peer-graph events one-for-one so a renderer that consumes the live stream and a renderer that polls this pull converge on byte-identical state.  DEFERRED-WIRING POSTURE: until the production composition root supplies the mesh-topology projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: mesh_topology_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param domain_id: Domain identifier (UUIDv7) — the topology scope. (required)
        :type domain_id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_domain_mesh_topology_serialize(
            domain_id=domain_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "MeshTopology",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _get_domain_mesh_topology_serialize(
        self,
        domain_id,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if domain_id is not None:
            _path_params['domainId'] = domain_id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/v1/domains/{domainId}/mesh/topology',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def get_node_events(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the SSE stream scope.")],
        last_event_id: Annotated[Optional[StrictStr], Field(description="SSE replay cursor. When present and parseable as a non-negative integer, the server resumes streaming from the envelope whose JetStream sequence is strictly greater than the supplied value. When absent or empty the stream tails from now. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> str:
        """Stream signed envelope events for a Node over SSE.

        Opens a Server-Sent Events stream of canonical signed `Envelope` records scoped to the addressed Node. Each `data:` frame is the JSON wire body produced by `internal/signing/envelope.CanonicalBytes`. The server runs ed25519 verification against the per-Domain signing public key before emitting the frame; consumers SHOULD verify the trailing `signature:` field again as a defence-in-depth measure.  The `id:` field on every event frame is the JetStream stream sequence number for that envelope. Clients resume by re- connecting with `Last-Event-ID: <numeric>` set to the last sequence they durably processed; the server replays from the next sequence (`> last`). When the header is absent or empty the stream tails from now — historical events are NOT backfilled.  The server emits a 25-second SSE comment-frame keep-alive (`:keep-alive\\n\\n`) so idle proxies do not collapse the connection. The `X-Plexsphere-API-Version` response header carries the contract version the stream conforms to; `Cache-Control: no-cache` opts the response out of any intermediary caching layer.  DEFERRED-WIRING POSTURE: the production composition root does not yet supply the SignatureVerifier, RelationChecker, or NodeRepo ports the handler depends on. Until those ports are wired, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned`. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the SSE stream scope. (required)
        :type id: UUID
        :param last_event_id: SSE replay cursor. When present and parseable as a non-negative integer, the server resumes streaming from the envelope whose JetStream sequence is strictly greater than the supplied value. When absent or empty the stream tails from now. 
        :type last_event_id: str
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_events_serialize(
            id=id,
            last_event_id=last_event_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "str",
            '400': "Problem",
            '401': "Problem",
            '403': "Problem",
            '404': "Problem",
            '503': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def get_node_events_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the SSE stream scope.")],
        last_event_id: Annotated[Optional[StrictStr], Field(description="SSE replay cursor. When present and parseable as a non-negative integer, the server resumes streaming from the envelope whose JetStream sequence is strictly greater than the supplied value. When absent or empty the stream tails from now. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[str]:
        """Stream signed envelope events for a Node over SSE.

        Opens a Server-Sent Events stream of canonical signed `Envelope` records scoped to the addressed Node. Each `data:` frame is the JSON wire body produced by `internal/signing/envelope.CanonicalBytes`. The server runs ed25519 verification against the per-Domain signing public key before emitting the frame; consumers SHOULD verify the trailing `signature:` field again as a defence-in-depth measure.  The `id:` field on every event frame is the JetStream stream sequence number for that envelope. Clients resume by re- connecting with `Last-Event-ID: <numeric>` set to the last sequence they durably processed; the server replays from the next sequence (`> last`). When the header is absent or empty the stream tails from now — historical events are NOT backfilled.  The server emits a 25-second SSE comment-frame keep-alive (`:keep-alive\\n\\n`) so idle proxies do not collapse the connection. The `X-Plexsphere-API-Version` response header carries the contract version the stream conforms to; `Cache-Control: no-cache` opts the response out of any intermediary caching layer.  DEFERRED-WIRING POSTURE: the production composition root does not yet supply the SignatureVerifier, RelationChecker, or NodeRepo ports the handler depends on. Until those ports are wired, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned`. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the SSE stream scope. (required)
        :type id: UUID
        :param last_event_id: SSE replay cursor. When present and parseable as a non-negative integer, the server resumes streaming from the envelope whose JetStream sequence is strictly greater than the supplied value. When absent or empty the stream tails from now. 
        :type last_event_id: str
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_events_serialize(
            id=id,
            last_event_id=last_event_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "str",
            '400': "Problem",
            '401': "Problem",
            '403': "Problem",
            '404': "Problem",
            '503': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def get_node_events_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the SSE stream scope.")],
        last_event_id: Annotated[Optional[StrictStr], Field(description="SSE replay cursor. When present and parseable as a non-negative integer, the server resumes streaming from the envelope whose JetStream sequence is strictly greater than the supplied value. When absent or empty the stream tails from now. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Stream signed envelope events for a Node over SSE.

        Opens a Server-Sent Events stream of canonical signed `Envelope` records scoped to the addressed Node. Each `data:` frame is the JSON wire body produced by `internal/signing/envelope.CanonicalBytes`. The server runs ed25519 verification against the per-Domain signing public key before emitting the frame; consumers SHOULD verify the trailing `signature:` field again as a defence-in-depth measure.  The `id:` field on every event frame is the JetStream stream sequence number for that envelope. Clients resume by re- connecting with `Last-Event-ID: <numeric>` set to the last sequence they durably processed; the server replays from the next sequence (`> last`). When the header is absent or empty the stream tails from now — historical events are NOT backfilled.  The server emits a 25-second SSE comment-frame keep-alive (`:keep-alive\\n\\n`) so idle proxies do not collapse the connection. The `X-Plexsphere-API-Version` response header carries the contract version the stream conforms to; `Cache-Control: no-cache` opts the response out of any intermediary caching layer.  DEFERRED-WIRING POSTURE: the production composition root does not yet supply the SignatureVerifier, RelationChecker, or NodeRepo ports the handler depends on. Until those ports are wired, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned`. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the SSE stream scope. (required)
        :type id: UUID
        :param last_event_id: SSE replay cursor. When present and parseable as a non-negative integer, the server resumes streaming from the envelope whose JetStream sequence is strictly greater than the supplied value. When absent or empty the stream tails from now. 
        :type last_event_id: str
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_events_serialize(
            id=id,
            last_event_id=last_event_id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "str",
            '400': "Problem",
            '401': "Problem",
            '403': "Problem",
            '404': "Problem",
            '503': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _get_node_events_serialize(
        self,
        id,
        last_event_id,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        if last_event_id is not None:
            _header_params['Last-Event-ID'] = last_event_id
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'text/event-stream', 
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/v1/nodes/{id}/events',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def get_node_keys_rotate_preview(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the rotation scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RotationImpactPreview:
        """Preview the fleet impact of rotating a Node's mesh key.

        Returns a non-mutating dry-run of the fleet impact a manual mesh-key rotation against the addressed Node would have. Backs the operator dashboard \"manual key-rotation with impact preview\" view and the `plexctl key rotate --preview` CLI. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the same operator-action ReBAC relation      `node-operate` on `node:<id>` that the mutating      `POST /v1/nodes/{id}/keys/rotate` checks, BEFORE any      existence check, so an unauthorised caller cannot probe      Node existence via the 404/403 timing differential. A      caller lacking the relation receives 403      `insufficient_relation` and an audit row with outcome      `insufficient_relation`.   3. Resolves the per-Node read-side projection — the live      Peer the rotation would target, every peer edge whose      PSK would be re-issued, and the in-flight rotation row      (if any). NO `peer_key_rotation` row is created, NO      `rotate_keys` outbox event is appended, and NO audit row      beyond the read-side ReBAC decision is emitted.  The 200 response payload mirrors the mutating endpoint's view of the world one-for-one — `peer_id` matches what the live `POST` would return, `already_pending` flips on a pre-existing in-flight rotation the same way — so a renderer that consumes both surfaces can drive the dashboard confirmation dialog from a single mapping.  DEFERRED-WIRING POSTURE: until the production composition root supplies the read-side preview projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_preview_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the rotation scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_keys_rotate_preview_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "RotationImpactPreview",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def get_node_keys_rotate_preview_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the rotation scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[RotationImpactPreview]:
        """Preview the fleet impact of rotating a Node's mesh key.

        Returns a non-mutating dry-run of the fleet impact a manual mesh-key rotation against the addressed Node would have. Backs the operator dashboard \"manual key-rotation with impact preview\" view and the `plexctl key rotate --preview` CLI. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the same operator-action ReBAC relation      `node-operate` on `node:<id>` that the mutating      `POST /v1/nodes/{id}/keys/rotate` checks, BEFORE any      existence check, so an unauthorised caller cannot probe      Node existence via the 404/403 timing differential. A      caller lacking the relation receives 403      `insufficient_relation` and an audit row with outcome      `insufficient_relation`.   3. Resolves the per-Node read-side projection — the live      Peer the rotation would target, every peer edge whose      PSK would be re-issued, and the in-flight rotation row      (if any). NO `peer_key_rotation` row is created, NO      `rotate_keys` outbox event is appended, and NO audit row      beyond the read-side ReBAC decision is emitted.  The 200 response payload mirrors the mutating endpoint's view of the world one-for-one — `peer_id` matches what the live `POST` would return, `already_pending` flips on a pre-existing in-flight rotation the same way — so a renderer that consumes both surfaces can drive the dashboard confirmation dialog from a single mapping.  DEFERRED-WIRING POSTURE: until the production composition root supplies the read-side preview projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_preview_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the rotation scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_keys_rotate_preview_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "RotationImpactPreview",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def get_node_keys_rotate_preview_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the rotation scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Preview the fleet impact of rotating a Node's mesh key.

        Returns a non-mutating dry-run of the fleet impact a manual mesh-key rotation against the addressed Node would have. Backs the operator dashboard \"manual key-rotation with impact preview\" view and the `plexctl key rotate --preview` CLI. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the same operator-action ReBAC relation      `node-operate` on `node:<id>` that the mutating      `POST /v1/nodes/{id}/keys/rotate` checks, BEFORE any      existence check, so an unauthorised caller cannot probe      Node existence via the 404/403 timing differential. A      caller lacking the relation receives 403      `insufficient_relation` and an audit row with outcome      `insufficient_relation`.   3. Resolves the per-Node read-side projection — the live      Peer the rotation would target, every peer edge whose      PSK would be re-issued, and the in-flight rotation row      (if any). NO `peer_key_rotation` row is created, NO      `rotate_keys` outbox event is appended, and NO audit row      beyond the read-side ReBAC decision is emitted.  The 200 response payload mirrors the mutating endpoint's view of the world one-for-one — `peer_id` matches what the live `POST` would return, `already_pending` flips on a pre-existing in-flight rotation the same way — so a renderer that consumes both surfaces can drive the dashboard confirmation dialog from a single mapping.  DEFERRED-WIRING POSTURE: until the production composition root supplies the read-side preview projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_preview_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the rotation scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_keys_rotate_preview_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "RotationImpactPreview",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _get_node_keys_rotate_preview_serialize(
        self,
        id,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/v1/nodes/{id}/keys/rotate/preview',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def get_node_reachability(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the reachability scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> Reachability:
        """Read the reachability projection for a Node.

        Returns the latest `Reachability` projection for the addressed Node — the per-Node state-machine the heartbeat handler advances on every accepted heartbeat. The projection is the authoritative health view the operator UI and reconciliation surface consume; it transitions `healthy` → `stale` after 90s without a heartbeat and `stale` → `unreachable` after 300s, with `changed_at` tracking the most recent transition.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/state` and `GET /v1/nodes/{id}/events`, so any caller authorised to issue a reconciliation pull is also authorised to read the reachability projection. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle .  DEFERRED-WIRING POSTURE: until the production composition root supplies the ReachabilityRepo, RelationChecker, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: reachability_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the reachability scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_reachability_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "Reachability",
            '401': "Problem",
            '403': "Problem",
            '404': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def get_node_reachability_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the reachability scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[Reachability]:
        """Read the reachability projection for a Node.

        Returns the latest `Reachability` projection for the addressed Node — the per-Node state-machine the heartbeat handler advances on every accepted heartbeat. The projection is the authoritative health view the operator UI and reconciliation surface consume; it transitions `healthy` → `stale` after 90s without a heartbeat and `stale` → `unreachable` after 300s, with `changed_at` tracking the most recent transition.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/state` and `GET /v1/nodes/{id}/events`, so any caller authorised to issue a reconciliation pull is also authorised to read the reachability projection. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle .  DEFERRED-WIRING POSTURE: until the production composition root supplies the ReachabilityRepo, RelationChecker, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: reachability_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the reachability scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_reachability_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "Reachability",
            '401': "Problem",
            '403': "Problem",
            '404': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def get_node_reachability_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the reachability scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Read the reachability projection for a Node.

        Returns the latest `Reachability` projection for the addressed Node — the per-Node state-machine the heartbeat handler advances on every accepted heartbeat. The projection is the authoritative health view the operator UI and reconciliation surface consume; it transitions `healthy` → `stale` after 90s without a heartbeat and `stale` → `unreachable` after 300s, with `changed_at` tracking the most recent transition.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/state` and `GET /v1/nodes/{id}/events`, so any caller authorised to issue a reconciliation pull is also authorised to read the reachability projection. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle .  DEFERRED-WIRING POSTURE: until the production composition root supplies the ReachabilityRepo, RelationChecker, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: reachability_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the reachability scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_reachability_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "Reachability",
            '401': "Problem",
            '403': "Problem",
            '404': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _get_node_reachability_serialize(
        self,
        id,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/v1/nodes/{id}/reachability',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def get_node_state(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the snapshot scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> NodeStateSnapshot:
        """Reconciliation pull for a Node — return the canonical NodeStateSnapshot.

        Returns the canonical `NodeStateSnapshot` for the addressed Node. The snapshot is the authoritative cold-start view that plexd consumes when it first comes up, when its SSE connection has been disconnected for longer than the replay window, or when an out-of-band request arrives to re-derive the desired state. The four wire blocks — `peers`, `policy`, `bridge`, and `state`/`reports` — are always present so plexd's reconcile loop can diff by field presence rather than absence; later stories populate the currently-empty blocks without changing the wire shape .  The peer projection is a single SQL round-trip ordered by `node_id ASC` so two consecutive pulls against the same ledger snapshot are byte-equal — plexd's reconcile loop reduces redundant rewrites by hashing the response.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/events`, so any caller authorised to subscribe to a Node's SSE event stream is also authorised to issue a reconciliation pull. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle.  DEFERRED-WIRING POSTURE: until the production composition root supplies the `SnapshotProvider`, `RelationChecker`, and `NodeRepo` ports the handler depends on, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the snapshot scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_state_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "NodeStateSnapshot",
            '401': "Problem",
            '403': "Problem",
            '404': "Problem",
            '503': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def get_node_state_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the snapshot scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[NodeStateSnapshot]:
        """Reconciliation pull for a Node — return the canonical NodeStateSnapshot.

        Returns the canonical `NodeStateSnapshot` for the addressed Node. The snapshot is the authoritative cold-start view that plexd consumes when it first comes up, when its SSE connection has been disconnected for longer than the replay window, or when an out-of-band request arrives to re-derive the desired state. The four wire blocks — `peers`, `policy`, `bridge`, and `state`/`reports` — are always present so plexd's reconcile loop can diff by field presence rather than absence; later stories populate the currently-empty blocks without changing the wire shape .  The peer projection is a single SQL round-trip ordered by `node_id ASC` so two consecutive pulls against the same ledger snapshot are byte-equal — plexd's reconcile loop reduces redundant rewrites by hashing the response.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/events`, so any caller authorised to subscribe to a Node's SSE event stream is also authorised to issue a reconciliation pull. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle.  DEFERRED-WIRING POSTURE: until the production composition root supplies the `SnapshotProvider`, `RelationChecker`, and `NodeRepo` ports the handler depends on, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the snapshot scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_state_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "NodeStateSnapshot",
            '401': "Problem",
            '403': "Problem",
            '404': "Problem",
            '503': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def get_node_state_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the snapshot scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Reconciliation pull for a Node — return the canonical NodeStateSnapshot.

        Returns the canonical `NodeStateSnapshot` for the addressed Node. The snapshot is the authoritative cold-start view that plexd consumes when it first comes up, when its SSE connection has been disconnected for longer than the replay window, or when an out-of-band request arrives to re-derive the desired state. The four wire blocks — `peers`, `policy`, `bridge`, and `state`/`reports` — are always present so plexd's reconcile loop can diff by field presence rather than absence; later stories populate the currently-empty blocks without changing the wire shape .  The peer projection is a single SQL round-trip ordered by `node_id ASC` so two consecutive pulls against the same ledger snapshot are byte-equal — plexd's reconcile loop reduces redundant rewrites by hashing the response.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/events`, so any caller authorised to subscribe to a Node's SSE event stream is also authorised to issue a reconciliation pull. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle.  DEFERRED-WIRING POSTURE: until the production composition root supplies the `SnapshotProvider`, `RelationChecker`, and `NodeRepo` ports the handler depends on, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the snapshot scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._get_node_state_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "NodeStateSnapshot",
            '401': "Problem",
            '403': "Problem",
            '404': "Problem",
            '503': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _get_node_state_serialize(
        self,
        id,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/v1/nodes/{id}/state',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def post_keys_rotate(
        self,
        keys_rotate_request: KeysRotateRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> KeysRotateResponse:
        """Submit a freshly-generated Curve25519 key to complete a rotation.

        Completes a pending mesh-key rotation for the calling Node. After an operator triggers a rotation via `POST /v1/nodes/{id}/keys/rotate` — or the heartbeat response sets `rotate_keys: true` — plexd generates a fresh Curve25519 keypair and submits the new public key here. The handler:    1. Authenticates the caller against the Node Secret Key      (NSK) plaintext supplied in the `Authorization: Bearer`      header. The route carries NO path `{id}` segment — the      rotating Node is identified solely by the NSK envelope it      presents — so a missing or unresolved NSK surfaces as      401.   2. Caps the request body at 4 KiB; oversize bodies receive      413 `keys_rotate_body_too_large` without ever touching      the JSON decoder.   3. Validates the submitted `new_public_key` — it MUST decode      to exactly 32 bytes and MUST NOT be the all-zero      degenerate value — BEFORE any database read (otherwise      422 `keys_rotate_public_key_invalid`).   4. Resolves the caller's live Peer row; a Node with no live      Peer surfaces as 404 `keys_rotate_peer_not_found`.   5. Hands the new key to the rotation completer, which — in      one transaction — updates `nodes.public_key`, retires the      old pairwise PSK, wraps and inserts a fresh PSK, appends      a `peer_key_rotated` outbox event, and flips the pending      `peer_key_rotation` row to `completed`.  The 200 response carries the completed rotation's id plus the `(kid, wrap_key_version)` reference of the re-issued PSK — it never carries PSK plaintext or ciphertext. An idempotent retry with the same key against an already-completed rotation returns the prior receipt and emits no second event.  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationCompleter, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param keys_rotate_request: (required)
        :type keys_rotate_request: KeysRotateRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_keys_rotate_serialize(
            keys_rotate_request=keys_rotate_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "KeysRotateResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '413': "Problem",
            '422': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def post_keys_rotate_with_http_info(
        self,
        keys_rotate_request: KeysRotateRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[KeysRotateResponse]:
        """Submit a freshly-generated Curve25519 key to complete a rotation.

        Completes a pending mesh-key rotation for the calling Node. After an operator triggers a rotation via `POST /v1/nodes/{id}/keys/rotate` — or the heartbeat response sets `rotate_keys: true` — plexd generates a fresh Curve25519 keypair and submits the new public key here. The handler:    1. Authenticates the caller against the Node Secret Key      (NSK) plaintext supplied in the `Authorization: Bearer`      header. The route carries NO path `{id}` segment — the      rotating Node is identified solely by the NSK envelope it      presents — so a missing or unresolved NSK surfaces as      401.   2. Caps the request body at 4 KiB; oversize bodies receive      413 `keys_rotate_body_too_large` without ever touching      the JSON decoder.   3. Validates the submitted `new_public_key` — it MUST decode      to exactly 32 bytes and MUST NOT be the all-zero      degenerate value — BEFORE any database read (otherwise      422 `keys_rotate_public_key_invalid`).   4. Resolves the caller's live Peer row; a Node with no live      Peer surfaces as 404 `keys_rotate_peer_not_found`.   5. Hands the new key to the rotation completer, which — in      one transaction — updates `nodes.public_key`, retires the      old pairwise PSK, wraps and inserts a fresh PSK, appends      a `peer_key_rotated` outbox event, and flips the pending      `peer_key_rotation` row to `completed`.  The 200 response carries the completed rotation's id plus the `(kid, wrap_key_version)` reference of the re-issued PSK — it never carries PSK plaintext or ciphertext. An idempotent retry with the same key against an already-completed rotation returns the prior receipt and emits no second event.  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationCompleter, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param keys_rotate_request: (required)
        :type keys_rotate_request: KeysRotateRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_keys_rotate_serialize(
            keys_rotate_request=keys_rotate_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "KeysRotateResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '413': "Problem",
            '422': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def post_keys_rotate_without_preload_content(
        self,
        keys_rotate_request: KeysRotateRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Submit a freshly-generated Curve25519 key to complete a rotation.

        Completes a pending mesh-key rotation for the calling Node. After an operator triggers a rotation via `POST /v1/nodes/{id}/keys/rotate` — or the heartbeat response sets `rotate_keys: true` — plexd generates a fresh Curve25519 keypair and submits the new public key here. The handler:    1. Authenticates the caller against the Node Secret Key      (NSK) plaintext supplied in the `Authorization: Bearer`      header. The route carries NO path `{id}` segment — the      rotating Node is identified solely by the NSK envelope it      presents — so a missing or unresolved NSK surfaces as      401.   2. Caps the request body at 4 KiB; oversize bodies receive      413 `keys_rotate_body_too_large` without ever touching      the JSON decoder.   3. Validates the submitted `new_public_key` — it MUST decode      to exactly 32 bytes and MUST NOT be the all-zero      degenerate value — BEFORE any database read (otherwise      422 `keys_rotate_public_key_invalid`).   4. Resolves the caller's live Peer row; a Node with no live      Peer surfaces as 404 `keys_rotate_peer_not_found`.   5. Hands the new key to the rotation completer, which — in      one transaction — updates `nodes.public_key`, retires the      old pairwise PSK, wraps and inserts a fresh PSK, appends      a `peer_key_rotated` outbox event, and flips the pending      `peer_key_rotation` row to `completed`.  The 200 response carries the completed rotation's id plus the `(kid, wrap_key_version)` reference of the re-issued PSK — it never carries PSK plaintext or ciphertext. An idempotent retry with the same key against an already-completed rotation returns the prior receipt and emits no second event.  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationCompleter, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param keys_rotate_request: (required)
        :type keys_rotate_request: KeysRotateRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_keys_rotate_serialize(
            keys_rotate_request=keys_rotate_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "KeysRotateResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '409': "Problem",
            '413': "Problem",
            '422': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _post_keys_rotate_serialize(
        self,
        keys_rotate_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter
        if keys_rotate_request is not None:
            _body_params = keys_rotate_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/keys/rotate',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def post_node_audit(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the ingest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        audit_event: List[AuditEvent],
        x_plexsphere_sent_at: Annotated[Optional[datetime], Field(description="RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> IngestReceipt:
        """Ingest a batch of normalised audit events for a Node.

        Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `AuditEvent` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki with a separate retention class and a distinct SIEM route. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `source` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: audit-ingest labels carry Domain, Project, and Node — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the ingest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param audit_event: (required)
        :type audit_event: List[AuditEvent]
        :param x_plexsphere_sent_at: RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. 
        :type x_plexsphere_sent_at: datetime
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_audit_serialize(
            id=id,
            authorization=authorization,
            audit_event=audit_event,
            x_plexsphere_sent_at=x_plexsphere_sent_at,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IngestReceipt",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '413': "Problem",
            '415': "Problem",
            '429': "Problem",
            '501': "Problem",
            '503': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def post_node_audit_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the ingest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        audit_event: List[AuditEvent],
        x_plexsphere_sent_at: Annotated[Optional[datetime], Field(description="RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[IngestReceipt]:
        """Ingest a batch of normalised audit events for a Node.

        Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `AuditEvent` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki with a separate retention class and a distinct SIEM route. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `source` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: audit-ingest labels carry Domain, Project, and Node — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the ingest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param audit_event: (required)
        :type audit_event: List[AuditEvent]
        :param x_plexsphere_sent_at: RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. 
        :type x_plexsphere_sent_at: datetime
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_audit_serialize(
            id=id,
            authorization=authorization,
            audit_event=audit_event,
            x_plexsphere_sent_at=x_plexsphere_sent_at,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IngestReceipt",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '413': "Problem",
            '415': "Problem",
            '429': "Problem",
            '501': "Problem",
            '503': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def post_node_audit_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the ingest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        audit_event: List[AuditEvent],
        x_plexsphere_sent_at: Annotated[Optional[datetime], Field(description="RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Ingest a batch of normalised audit events for a Node.

        Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `AuditEvent` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki with a separate retention class and a distinct SIEM route. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `source` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: audit-ingest labels carry Domain, Project, and Node — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the ingest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param audit_event: (required)
        :type audit_event: List[AuditEvent]
        :param x_plexsphere_sent_at: RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. 
        :type x_plexsphere_sent_at: datetime
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_audit_serialize(
            id=id,
            authorization=authorization,
            audit_event=audit_event,
            x_plexsphere_sent_at=x_plexsphere_sent_at,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IngestReceipt",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '413': "Problem",
            '415': "Problem",
            '429': "Problem",
            '501': "Problem",
            '503': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _post_node_audit_serialize(
        self,
        id,
        authorization,
        audit_event,
        x_plexsphere_sent_at,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
            'AuditEvent': '',
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        if x_plexsphere_sent_at is not None:
            _header_params['X-Plexsphere-Sent-At'] = x_plexsphere_sent_at
        # process the form parameters
        # process the body parameter
        if audit_event is not None:
            _body_params = audit_event


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/x-ndjson'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/nodes/{id}/audit',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def post_node_heartbeat(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the heartbeat scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch` . ")],
        heartbeat_request: HeartbeatRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> HeartbeatResponse:
        """Record a Node liveness heartbeat and return reconcile/rotate hints.

        Accepts a per-Node liveness heartbeat from plexd and updates the reachability projection that drives the mesh-wide health view . The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      rejecting revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Validates the request body — `client_now` MUST be within      60s of server now (otherwise 400 `clock_skew`),      `binary_checksum` MUST be present and decode to a 32-byte      SHA-256 digest (otherwise 400 `binary_checksum_empty`)      .   4. Persists the heartbeat fact and updates the per-Node      reachability state-machine (`healthy` → `stale` after 90s,      `stale` → `unreachable` after 300s) so the projection at      `GET /v1/nodes/{id}/reachability` reflects the new fact      on the next read.  The 200 response carries `accepted_at` (server timestamp at commit) and two reconciliation flags. `reconcile` defaults to `false` and later stories flip it when the controller wants plexd to issue a fresh reconciliation pull. `rotate_keys` is load-bearing: it is set to `true` whenever a `peer_key_rotation` row is pending for the heartbeating Node, telling the caller to generate a fresh Curve25519 keypair and complete the rotation via `POST /v1/keys/rotate`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the NSK validator, ReachabilityRepo, and clock-skew evaluator the handler depends on, every request to this endpoint returns 501 with `code: heartbeat_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the heartbeat scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch` .  (required)
        :type authorization: str
        :param heartbeat_request: (required)
        :type heartbeat_request: HeartbeatRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_heartbeat_serialize(
            id=id,
            authorization=authorization,
            heartbeat_request=heartbeat_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "HeartbeatResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def post_node_heartbeat_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the heartbeat scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch` . ")],
        heartbeat_request: HeartbeatRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[HeartbeatResponse]:
        """Record a Node liveness heartbeat and return reconcile/rotate hints.

        Accepts a per-Node liveness heartbeat from plexd and updates the reachability projection that drives the mesh-wide health view . The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      rejecting revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Validates the request body — `client_now` MUST be within      60s of server now (otherwise 400 `clock_skew`),      `binary_checksum` MUST be present and decode to a 32-byte      SHA-256 digest (otherwise 400 `binary_checksum_empty`)      .   4. Persists the heartbeat fact and updates the per-Node      reachability state-machine (`healthy` → `stale` after 90s,      `stale` → `unreachable` after 300s) so the projection at      `GET /v1/nodes/{id}/reachability` reflects the new fact      on the next read.  The 200 response carries `accepted_at` (server timestamp at commit) and two reconciliation flags. `reconcile` defaults to `false` and later stories flip it when the controller wants plexd to issue a fresh reconciliation pull. `rotate_keys` is load-bearing: it is set to `true` whenever a `peer_key_rotation` row is pending for the heartbeating Node, telling the caller to generate a fresh Curve25519 keypair and complete the rotation via `POST /v1/keys/rotate`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the NSK validator, ReachabilityRepo, and clock-skew evaluator the handler depends on, every request to this endpoint returns 501 with `code: heartbeat_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the heartbeat scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch` .  (required)
        :type authorization: str
        :param heartbeat_request: (required)
        :type heartbeat_request: HeartbeatRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_heartbeat_serialize(
            id=id,
            authorization=authorization,
            heartbeat_request=heartbeat_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "HeartbeatResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def post_node_heartbeat_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the heartbeat scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch` . ")],
        heartbeat_request: HeartbeatRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Record a Node liveness heartbeat and return reconcile/rotate hints.

        Accepts a per-Node liveness heartbeat from plexd and updates the reachability projection that drives the mesh-wide health view . The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      rejecting revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Validates the request body — `client_now` MUST be within      60s of server now (otherwise 400 `clock_skew`),      `binary_checksum` MUST be present and decode to a 32-byte      SHA-256 digest (otherwise 400 `binary_checksum_empty`)      .   4. Persists the heartbeat fact and updates the per-Node      reachability state-machine (`healthy` → `stale` after 90s,      `stale` → `unreachable` after 300s) so the projection at      `GET /v1/nodes/{id}/reachability` reflects the new fact      on the next read.  The 200 response carries `accepted_at` (server timestamp at commit) and two reconciliation flags. `reconcile` defaults to `false` and later stories flip it when the controller wants plexd to issue a fresh reconciliation pull. `rotate_keys` is load-bearing: it is set to `true` whenever a `peer_key_rotation` row is pending for the heartbeating Node, telling the caller to generate a fresh Curve25519 keypair and complete the rotation via `POST /v1/keys/rotate`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the NSK validator, ReachabilityRepo, and clock-skew evaluator the handler depends on, every request to this endpoint returns 501 with `code: heartbeat_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking. 

        :param id: Node identifier (UUIDv7) — the heartbeat scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch` .  (required)
        :type authorization: str
        :param heartbeat_request: (required)
        :type heartbeat_request: HeartbeatRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_heartbeat_serialize(
            id=id,
            authorization=authorization,
            heartbeat_request=heartbeat_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "HeartbeatResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _post_node_heartbeat_serialize(
        self,
        id,
        authorization,
        heartbeat_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        # process the form parameters
        # process the body parameter
        if heartbeat_request is not None:
            _body_params = heartbeat_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/nodes/{id}/heartbeat',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def post_node_integrity_violations(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the reporting Node.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        integrity_violations_request: IntegrityViolationsRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> IntegrityViolationsResponse:
        """Ingest a batch of integrity-violation reports for a Node.

        Accepts a batch of `IntegrityViolation` reports from plexd describing tamper-evidence divergences the agent detected on the local Node: a mismatched binary checksum, a tampered Lua hook checksum, or a rotated SSH host-key fingerprint. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `integrity_violations_body_too_large` without ever      touching the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_integrity_violations_request`.   5. Canonicalises every entry through the      `tenancy.NewIntegrityViolation` value-object constructor      which enforces every per-violation invariant: `kind` in      the closed set `{binary_checksum, hook_checksum,      ssh_host_key}`, `detected_by` in the closed set      `{startup_scan, inotify, pre_dispatch}`, `artifact_id`      non-empty after trimming, `observed_checksum`/      `expected_checksum` exactly 32 bytes for the checksum      kinds, `observed_fingerprint`/`expected_fingerprint`      matching `SHA256:<base64>` for the `ssh_host_key` kind,      and the per-kind column-mismatch guard that rejects a      checksum kind carrying fingerprint fields and vice versa.   6. Hands the canonical batch to the Node aggregate's      `RecordIntegrityViolations` method which enforces the      batch-level invariants: at least one entry      (`integrity_violations_empty`) and at most 128 entries      (`integrity_violations_too_many`).   7. Persists every violation row and appends a single      `integrity_alert` outbox event inside one transaction so      the operator signal and the audit evidence land together      or neither lands.  The 202 response carries `accepted_at` (server commit timestamp) and `violation_count` (echo of the persisted batch size) so the agent can record the receipt and reconcile with its local replay-queue. The response carries no payload echo and no per-row identifiers; the operator UI consumes the persisted rows from the dedicated audit and integrity surfaces.  DEFERRED-WIRING POSTURE: until the production composition root supplies the IntegrityViolationsRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: integrity_violations_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the reporting Node. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param integrity_violations_request: (required)
        :type integrity_violations_request: IntegrityViolationsRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_integrity_violations_serialize(
            id=id,
            authorization=authorization,
            integrity_violations_request=integrity_violations_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IntegrityViolationsResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '413': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def post_node_integrity_violations_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the reporting Node.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        integrity_violations_request: IntegrityViolationsRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[IntegrityViolationsResponse]:
        """Ingest a batch of integrity-violation reports for a Node.

        Accepts a batch of `IntegrityViolation` reports from plexd describing tamper-evidence divergences the agent detected on the local Node: a mismatched binary checksum, a tampered Lua hook checksum, or a rotated SSH host-key fingerprint. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `integrity_violations_body_too_large` without ever      touching the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_integrity_violations_request`.   5. Canonicalises every entry through the      `tenancy.NewIntegrityViolation` value-object constructor      which enforces every per-violation invariant: `kind` in      the closed set `{binary_checksum, hook_checksum,      ssh_host_key}`, `detected_by` in the closed set      `{startup_scan, inotify, pre_dispatch}`, `artifact_id`      non-empty after trimming, `observed_checksum`/      `expected_checksum` exactly 32 bytes for the checksum      kinds, `observed_fingerprint`/`expected_fingerprint`      matching `SHA256:<base64>` for the `ssh_host_key` kind,      and the per-kind column-mismatch guard that rejects a      checksum kind carrying fingerprint fields and vice versa.   6. Hands the canonical batch to the Node aggregate's      `RecordIntegrityViolations` method which enforces the      batch-level invariants: at least one entry      (`integrity_violations_empty`) and at most 128 entries      (`integrity_violations_too_many`).   7. Persists every violation row and appends a single      `integrity_alert` outbox event inside one transaction so      the operator signal and the audit evidence land together      or neither lands.  The 202 response carries `accepted_at` (server commit timestamp) and `violation_count` (echo of the persisted batch size) so the agent can record the receipt and reconcile with its local replay-queue. The response carries no payload echo and no per-row identifiers; the operator UI consumes the persisted rows from the dedicated audit and integrity surfaces.  DEFERRED-WIRING POSTURE: until the production composition root supplies the IntegrityViolationsRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: integrity_violations_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the reporting Node. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param integrity_violations_request: (required)
        :type integrity_violations_request: IntegrityViolationsRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_integrity_violations_serialize(
            id=id,
            authorization=authorization,
            integrity_violations_request=integrity_violations_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IntegrityViolationsResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '413': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def post_node_integrity_violations_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the reporting Node.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        integrity_violations_request: IntegrityViolationsRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Ingest a batch of integrity-violation reports for a Node.

        Accepts a batch of `IntegrityViolation` reports from plexd describing tamper-evidence divergences the agent detected on the local Node: a mismatched binary checksum, a tampered Lua hook checksum, or a rotated SSH host-key fingerprint. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `integrity_violations_body_too_large` without ever      touching the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_integrity_violations_request`.   5. Canonicalises every entry through the      `tenancy.NewIntegrityViolation` value-object constructor      which enforces every per-violation invariant: `kind` in      the closed set `{binary_checksum, hook_checksum,      ssh_host_key}`, `detected_by` in the closed set      `{startup_scan, inotify, pre_dispatch}`, `artifact_id`      non-empty after trimming, `observed_checksum`/      `expected_checksum` exactly 32 bytes for the checksum      kinds, `observed_fingerprint`/`expected_fingerprint`      matching `SHA256:<base64>` for the `ssh_host_key` kind,      and the per-kind column-mismatch guard that rejects a      checksum kind carrying fingerprint fields and vice versa.   6. Hands the canonical batch to the Node aggregate's      `RecordIntegrityViolations` method which enforces the      batch-level invariants: at least one entry      (`integrity_violations_empty`) and at most 128 entries      (`integrity_violations_too_many`).   7. Persists every violation row and appends a single      `integrity_alert` outbox event inside one transaction so      the operator signal and the audit evidence land together      or neither lands.  The 202 response carries `accepted_at` (server commit timestamp) and `violation_count` (echo of the persisted batch size) so the agent can record the receipt and reconcile with its local replay-queue. The response carries no payload echo and no per-row identifiers; the operator UI consumes the persisted rows from the dedicated audit and integrity surfaces.  DEFERRED-WIRING POSTURE: until the production composition root supplies the IntegrityViolationsRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: integrity_violations_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the reporting Node. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param integrity_violations_request: (required)
        :type integrity_violations_request: IntegrityViolationsRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_integrity_violations_serialize(
            id=id,
            authorization=authorization,
            integrity_violations_request=integrity_violations_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IntegrityViolationsResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '413': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _post_node_integrity_violations_serialize(
        self,
        id,
        authorization,
        integrity_violations_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        # process the form parameters
        # process the body parameter
        if integrity_violations_request is not None:
            _body_params = integrity_violations_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/nodes/{id}/integrity-violations',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def post_node_keys_rotate(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the rotation scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RotationTriggerResponse:
        """Trigger a mesh-key rotation for a Node.

        Asks the control plane to dispatch a `rotate_keys` command to the addressed Node so it generates a fresh Curve25519 keypair without re-enrolling from scratch. An operator calls this when a Node's mesh key is aged or possibly compromised. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the operator-action ReBAC relation `node-operate`      on `node:<id>` BEFORE any existence check, so an      unauthorised caller cannot probe Node existence via the      404/403 timing differential. A caller lacking the      relation receives 403 `insufficient_relation` and an      audit row with outcome `insufficient_relation`.   3. Records a pending `peer_key_rotation` row and appends      exactly one `rotate_keys` outbox event in a single      transaction. A second trigger against a Node that already      has a pending rotation returns the existing rotation      handle and appends no second event — the operation is      idempotent.  The 200 response carries the rotation handle: the `peer_key_rotation` row id, the live Peer id, and `already_pending` (true when the response reflects a pre-existing in-flight rotation rather than a fresh request).  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationRequester and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the rotation scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_keys_rotate_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "RotationTriggerResponse",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def post_node_keys_rotate_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the rotation scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[RotationTriggerResponse]:
        """Trigger a mesh-key rotation for a Node.

        Asks the control plane to dispatch a `rotate_keys` command to the addressed Node so it generates a fresh Curve25519 keypair without re-enrolling from scratch. An operator calls this when a Node's mesh key is aged or possibly compromised. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the operator-action ReBAC relation `node-operate`      on `node:<id>` BEFORE any existence check, so an      unauthorised caller cannot probe Node existence via the      404/403 timing differential. A caller lacking the      relation receives 403 `insufficient_relation` and an      audit row with outcome `insufficient_relation`.   3. Records a pending `peer_key_rotation` row and appends      exactly one `rotate_keys` outbox event in a single      transaction. A second trigger against a Node that already      has a pending rotation returns the existing rotation      handle and appends no second event — the operation is      idempotent.  The 200 response carries the rotation handle: the `peer_key_rotation` row id, the live Peer id, and `already_pending` (true when the response reflects a pre-existing in-flight rotation rather than a fresh request).  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationRequester and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the rotation scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_keys_rotate_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "RotationTriggerResponse",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def post_node_keys_rotate_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the rotation scope.")],
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Trigger a mesh-key rotation for a Node.

        Asks the control plane to dispatch a `rotate_keys` command to the addressed Node so it generates a fresh Curve25519 keypair without re-enrolling from scratch. An operator calls this when a Node's mesh key is aged or possibly compromised. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the operator-action ReBAC relation `node-operate`      on `node:<id>` BEFORE any existence check, so an      unauthorised caller cannot probe Node existence via the      404/403 timing differential. A caller lacking the      relation receives 403 `insufficient_relation` and an      audit row with outcome `insufficient_relation`.   3. Records a pending `peer_key_rotation` row and appends      exactly one `rotate_keys` outbox event in a single      transaction. A second trigger against a Node that already      has a pending rotation returns the existing rotation      handle and appends no second event — the operation is      idempotent.  The 200 response carries the rotation handle: the `peer_key_rotation` row id, the live Peer id, and `already_pending` (true when the response reflects a pre-existing in-flight rotation rather than a fresh request).  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationRequester and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the rotation scope. (required)
        :type id: UUID
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_keys_rotate_serialize(
            id=id,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "RotationTriggerResponse",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '500': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _post_node_keys_rotate_serialize(
        self,
        id,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )


        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/nodes/{id}/keys/rotate',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def post_node_logs(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the ingest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        log_line: List[LogLine],
        x_plexsphere_sent_at: Annotated[Optional[datetime], Field(description="RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> IngestReceipt:
        """Ingest a batch of structured log lines for a Node.

        Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `LogLine` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki plus optional SIEM forwarding. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `severity` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: log labels carry Domain, Project, and Node — never an Identity subject or email. Log lines that inadvertently contain PII are the customer's ingest-side responsibility; an optional per-Domain regex redaction filter, disabled by default, can be enabled at ingest.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the ingest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param log_line: (required)
        :type log_line: List[LogLine]
        :param x_plexsphere_sent_at: RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. 
        :type x_plexsphere_sent_at: datetime
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_logs_serialize(
            id=id,
            authorization=authorization,
            log_line=log_line,
            x_plexsphere_sent_at=x_plexsphere_sent_at,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IngestReceipt",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '413': "Problem",
            '415': "Problem",
            '429': "Problem",
            '501': "Problem",
            '503': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def post_node_logs_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the ingest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        log_line: List[LogLine],
        x_plexsphere_sent_at: Annotated[Optional[datetime], Field(description="RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[IngestReceipt]:
        """Ingest a batch of structured log lines for a Node.

        Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `LogLine` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki plus optional SIEM forwarding. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `severity` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: log labels carry Domain, Project, and Node — never an Identity subject or email. Log lines that inadvertently contain PII are the customer's ingest-side responsibility; an optional per-Domain regex redaction filter, disabled by default, can be enabled at ingest.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the ingest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param log_line: (required)
        :type log_line: List[LogLine]
        :param x_plexsphere_sent_at: RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. 
        :type x_plexsphere_sent_at: datetime
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_logs_serialize(
            id=id,
            authorization=authorization,
            log_line=log_line,
            x_plexsphere_sent_at=x_plexsphere_sent_at,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IngestReceipt",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '413': "Problem",
            '415': "Problem",
            '429': "Problem",
            '501': "Problem",
            '503': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def post_node_logs_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the ingest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        log_line: List[LogLine],
        x_plexsphere_sent_at: Annotated[Optional[datetime], Field(description="RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Ingest a batch of structured log lines for a Node.

        Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `LogLine` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki plus optional SIEM forwarding. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `severity` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: log labels carry Domain, Project, and Node — never an Identity subject or email. Log lines that inadvertently contain PII are the customer's ingest-side responsibility; an optional per-Domain regex redaction filter, disabled by default, can be enabled at ingest.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the ingest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param log_line: (required)
        :type log_line: List[LogLine]
        :param x_plexsphere_sent_at: RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. 
        :type x_plexsphere_sent_at: datetime
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_logs_serialize(
            id=id,
            authorization=authorization,
            log_line=log_line,
            x_plexsphere_sent_at=x_plexsphere_sent_at,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IngestReceipt",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '413': "Problem",
            '415': "Problem",
            '429': "Problem",
            '501': "Problem",
            '503': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _post_node_logs_serialize(
        self,
        id,
        authorization,
        log_line,
        x_plexsphere_sent_at,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
            'LogLine': '',
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        if x_plexsphere_sent_at is not None:
            _header_params['X-Plexsphere-Sent-At'] = x_plexsphere_sent_at
        # process the form parameters
        # process the body parameter
        if log_line is not None:
            _body_params = log_line


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/x-ndjson'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/nodes/{id}/logs',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def post_node_metrics(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the ingest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        metric_sample: List[MetricSample],
        x_plexsphere_sent_at: Annotated[Optional[datetime], Field(description="RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> IngestReceipt:
        """Ingest a batch of metric samples for a Node.

        Accepts a gzip-compressed JSON array of `MetricSample` records pushed by plexd and hands the batch to the per-signal ingest buffer — a JetStream subject keyed by the originating Node's Domain — from which the platform fans out to Grafana Mimir via Prometheus remote-write. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each record (a malformed record or      a `group` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: metric labels carry Domain, Project, Node, and (where relevant) workload identifier — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the ingest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param metric_sample: (required)
        :type metric_sample: List[MetricSample]
        :param x_plexsphere_sent_at: RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. 
        :type x_plexsphere_sent_at: datetime
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_metrics_serialize(
            id=id,
            authorization=authorization,
            metric_sample=metric_sample,
            x_plexsphere_sent_at=x_plexsphere_sent_at,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IngestReceipt",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '413': "Problem",
            '415': "Problem",
            '429': "Problem",
            '501': "Problem",
            '503': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def post_node_metrics_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the ingest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        metric_sample: List[MetricSample],
        x_plexsphere_sent_at: Annotated[Optional[datetime], Field(description="RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[IngestReceipt]:
        """Ingest a batch of metric samples for a Node.

        Accepts a gzip-compressed JSON array of `MetricSample` records pushed by plexd and hands the batch to the per-signal ingest buffer — a JetStream subject keyed by the originating Node's Domain — from which the platform fans out to Grafana Mimir via Prometheus remote-write. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each record (a malformed record or      a `group` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: metric labels carry Domain, Project, Node, and (where relevant) workload identifier — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the ingest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param metric_sample: (required)
        :type metric_sample: List[MetricSample]
        :param x_plexsphere_sent_at: RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. 
        :type x_plexsphere_sent_at: datetime
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_metrics_serialize(
            id=id,
            authorization=authorization,
            metric_sample=metric_sample,
            x_plexsphere_sent_at=x_plexsphere_sent_at,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IngestReceipt",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '413': "Problem",
            '415': "Problem",
            '429': "Problem",
            '501': "Problem",
            '503': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def post_node_metrics_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the ingest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        metric_sample: List[MetricSample],
        x_plexsphere_sent_at: Annotated[Optional[datetime], Field(description="RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. ")] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Ingest a batch of metric samples for a Node.

        Accepts a gzip-compressed JSON array of `MetricSample` records pushed by plexd and hands the batch to the per-signal ingest buffer — a JetStream subject keyed by the originating Node's Domain — from which the platform fans out to Grafana Mimir via Prometheus remote-write. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each record (a malformed record or      a `group` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: metric labels carry Domain, Project, Node, and (where relevant) workload identifier — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the ingest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param metric_sample: (required)
        :type metric_sample: List[MetricSample]
        :param x_plexsphere_sent_at: RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 `ingest_sent_at_invalid`; it drives the ingest-lag metric the platform records on acceptance. 
        :type x_plexsphere_sent_at: datetime
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._post_node_metrics_serialize(
            id=id,
            authorization=authorization,
            metric_sample=metric_sample,
            x_plexsphere_sent_at=x_plexsphere_sent_at,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '202': "IngestReceipt",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '413': "Problem",
            '415': "Problem",
            '429': "Problem",
            '501': "Problem",
            '503': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _post_node_metrics_serialize(
        self,
        id,
        authorization,
        metric_sample,
        x_plexsphere_sent_at,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
            'MetricSample': '',
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        if x_plexsphere_sent_at is not None:
            _header_params['X-Plexsphere-Sent-At'] = x_plexsphere_sent_at
        # process the form parameters
        # process the body parameter
        if metric_sample is not None:
            _body_params = metric_sample


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/v1/nodes/{id}/metrics',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def put_node_capabilities(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the capability manifest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        capability_manifest_request: CapabilityManifestRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> CapabilityManifestResponse:
        """Record the per-Node capability manifest snapshot.

        Accepts a CapabilityManifest snapshot from plexd describing the running agent binary, its SSH host-key fingerprint, and any hooks the agent advertises. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `capabilities_body_too_large` without ever touching      the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_capabilities_request`.   5. Canonicalises the envelope through the      `tenancy.NewCapabilityManifest` value-object constructor      which enforces every manifest invariant: `binary_version`      non-empty, `binary_checksum` exactly 32 bytes,      `ssh_host_key_fingerprint` empty or matching      `SHA256:<base64>`, no duplicate hook names, hook count      within the per-manifest cap, and every hook's `checksum`      exactly 32 bytes.   6. Persists the snapshot inside a single transaction. When      the supplied manifest differs from the prior persisted      row, the recorder appends a `NodeCapabilitiesUpdated`      outbox event in the same transaction so downstream      integrity correlators and dashboard projectors observe      the change atomically.  The 200 response carries `accepted_at` (server commit timestamp), the alphabetically-sorted `fields_changed` list of manifest fields that transitioned versus the prior snapshot, and the dedicated `host_key_changed` flag so downstream security correlators can branch on SSH host-key transitions without parsing `fields_changed`. An idempotent PUT (no diff) emits an empty `fields_changed` array and `host_key_changed: false`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the CapabilitiesRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: capabilities_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the capability manifest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param capability_manifest_request: (required)
        :type capability_manifest_request: CapabilityManifestRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._put_node_capabilities_serialize(
            id=id,
            authorization=authorization,
            capability_manifest_request=capability_manifest_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CapabilityManifestResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '413': "Problem",
            '422': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def put_node_capabilities_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the capability manifest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        capability_manifest_request: CapabilityManifestRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[CapabilityManifestResponse]:
        """Record the per-Node capability manifest snapshot.

        Accepts a CapabilityManifest snapshot from plexd describing the running agent binary, its SSH host-key fingerprint, and any hooks the agent advertises. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `capabilities_body_too_large` without ever touching      the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_capabilities_request`.   5. Canonicalises the envelope through the      `tenancy.NewCapabilityManifest` value-object constructor      which enforces every manifest invariant: `binary_version`      non-empty, `binary_checksum` exactly 32 bytes,      `ssh_host_key_fingerprint` empty or matching      `SHA256:<base64>`, no duplicate hook names, hook count      within the per-manifest cap, and every hook's `checksum`      exactly 32 bytes.   6. Persists the snapshot inside a single transaction. When      the supplied manifest differs from the prior persisted      row, the recorder appends a `NodeCapabilitiesUpdated`      outbox event in the same transaction so downstream      integrity correlators and dashboard projectors observe      the change atomically.  The 200 response carries `accepted_at` (server commit timestamp), the alphabetically-sorted `fields_changed` list of manifest fields that transitioned versus the prior snapshot, and the dedicated `host_key_changed` flag so downstream security correlators can branch on SSH host-key transitions without parsing `fields_changed`. An idempotent PUT (no diff) emits an empty `fields_changed` array and `host_key_changed: false`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the CapabilitiesRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: capabilities_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the capability manifest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param capability_manifest_request: (required)
        :type capability_manifest_request: CapabilityManifestRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._put_node_capabilities_serialize(
            id=id,
            authorization=authorization,
            capability_manifest_request=capability_manifest_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CapabilityManifestResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '413': "Problem",
            '422': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def put_node_capabilities_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the capability manifest scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        capability_manifest_request: CapabilityManifestRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Record the per-Node capability manifest snapshot.

        Accepts a CapabilityManifest snapshot from plexd describing the running agent binary, its SSH host-key fingerprint, and any hooks the agent advertises. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `capabilities_body_too_large` without ever touching      the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_capabilities_request`.   5. Canonicalises the envelope through the      `tenancy.NewCapabilityManifest` value-object constructor      which enforces every manifest invariant: `binary_version`      non-empty, `binary_checksum` exactly 32 bytes,      `ssh_host_key_fingerprint` empty or matching      `SHA256:<base64>`, no duplicate hook names, hook count      within the per-manifest cap, and every hook's `checksum`      exactly 32 bytes.   6. Persists the snapshot inside a single transaction. When      the supplied manifest differs from the prior persisted      row, the recorder appends a `NodeCapabilitiesUpdated`      outbox event in the same transaction so downstream      integrity correlators and dashboard projectors observe      the change atomically.  The 200 response carries `accepted_at` (server commit timestamp), the alphabetically-sorted `fields_changed` list of manifest fields that transitioned versus the prior snapshot, and the dedicated `host_key_changed` flag so downstream security correlators can branch on SSH host-key transitions without parsing `fields_changed`. An idempotent PUT (no diff) emits an empty `fields_changed` array and `host_key_changed: false`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the CapabilitiesRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: capabilities_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the capability manifest scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param capability_manifest_request: (required)
        :type capability_manifest_request: CapabilityManifestRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._put_node_capabilities_serialize(
            id=id,
            authorization=authorization,
            capability_manifest_request=capability_manifest_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CapabilityManifestResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '413': "Problem",
            '422': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _put_node_capabilities_serialize(
        self,
        id,
        authorization,
        capability_manifest_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        # process the form parameters
        # process the body parameter
        if capability_manifest_request is not None:
            _body_params = capability_manifest_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='PUT',
            resource_path='/v1/nodes/{id}/capabilities',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def put_node_endpoint(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the endpoint scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        endpoint_request: EndpointRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> EndpointResponse:
        """Record a NAT-observed Peer endpoint observation.

        Accepts a per-Node NAT endpoint observation from plexd and stamps it onto the Peer aggregate that backs the addressed Node. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 4 KiB; oversize bodies receive      413 `endpoint_body_too_large` without ever touching the      JSON decoder.   4. Validates the body — `reported_at` MUST be within 60s of      server now (otherwise 400 `endpoint_clock_skew`),      `endpoint` MUST parse as a `host:port` tuple in the      RFC 6056 1..65535 port range and a non-loopback IPv4/IPv6      address (otherwise 400 `endpoint_unparseable`), and the      envelope MUST decode without unknown fields (otherwise      400 `malformed_endpoint_request`).   5. Persists the observation through the per-Domain Peer      aggregate writer, which emits a `peer_endpoint_changed`      outbox event when the (endpoint, port) tuple differs from      the prior observation or transitions out of the stale      window; identical refreshes simply refresh the freshness      timestamp without emitting a wake-up.  The 200 response carries `accepted_at` (server commit timestamp) and `stale_after` (`accepted_at` plus the per-Domain endpoint TTL); plexd uses `stale_after` to schedule the next observation before the sweeper would tombstone the endpoint.  DEFERRED-WIRING POSTURE: until the production composition root supplies the EndpointRecorder, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: endpoint_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the endpoint scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param endpoint_request: (required)
        :type endpoint_request: EndpointRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._put_node_endpoint_serialize(
            id=id,
            authorization=authorization,
            endpoint_request=endpoint_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "EndpointResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '410': "Problem",
            '413': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def put_node_endpoint_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the endpoint scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        endpoint_request: EndpointRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[EndpointResponse]:
        """Record a NAT-observed Peer endpoint observation.

        Accepts a per-Node NAT endpoint observation from plexd and stamps it onto the Peer aggregate that backs the addressed Node. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 4 KiB; oversize bodies receive      413 `endpoint_body_too_large` without ever touching the      JSON decoder.   4. Validates the body — `reported_at` MUST be within 60s of      server now (otherwise 400 `endpoint_clock_skew`),      `endpoint` MUST parse as a `host:port` tuple in the      RFC 6056 1..65535 port range and a non-loopback IPv4/IPv6      address (otherwise 400 `endpoint_unparseable`), and the      envelope MUST decode without unknown fields (otherwise      400 `malformed_endpoint_request`).   5. Persists the observation through the per-Domain Peer      aggregate writer, which emits a `peer_endpoint_changed`      outbox event when the (endpoint, port) tuple differs from      the prior observation or transitions out of the stale      window; identical refreshes simply refresh the freshness      timestamp without emitting a wake-up.  The 200 response carries `accepted_at` (server commit timestamp) and `stale_after` (`accepted_at` plus the per-Domain endpoint TTL); plexd uses `stale_after` to schedule the next observation before the sweeper would tombstone the endpoint.  DEFERRED-WIRING POSTURE: until the production composition root supplies the EndpointRecorder, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: endpoint_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the endpoint scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param endpoint_request: (required)
        :type endpoint_request: EndpointRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._put_node_endpoint_serialize(
            id=id,
            authorization=authorization,
            endpoint_request=endpoint_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "EndpointResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '410': "Problem",
            '413': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def put_node_endpoint_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the endpoint scope.")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`. ")],
        endpoint_request: EndpointRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Record a NAT-observed Peer endpoint observation.

        Accepts a per-Node NAT endpoint observation from plexd and stamps it onto the Peer aggregate that backs the addressed Node. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 4 KiB; oversize bodies receive      413 `endpoint_body_too_large` without ever touching the      JSON decoder.   4. Validates the body — `reported_at` MUST be within 60s of      server now (otherwise 400 `endpoint_clock_skew`),      `endpoint` MUST parse as a `host:port` tuple in the      RFC 6056 1..65535 port range and a non-loopback IPv4/IPv6      address (otherwise 400 `endpoint_unparseable`), and the      envelope MUST decode without unknown fields (otherwise      400 `malformed_endpoint_request`).   5. Persists the observation through the per-Domain Peer      aggregate writer, which emits a `peer_endpoint_changed`      outbox event when the (endpoint, port) tuple differs from      the prior observation or transitions out of the stale      window; identical refreshes simply refresh the freshness      timestamp without emitting a wake-up.  The 200 response carries `accepted_at` (server commit timestamp) and `stale_after` (`accepted_at` plus the per-Domain endpoint TTL); plexd uses `stale_after` to schedule the next observation before the sweeper would tombstone the endpoint.  DEFERRED-WIRING POSTURE: until the production composition root supplies the EndpointRecorder, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: endpoint_not_provisioned` so log scrapers can alert on the deferred-wiring state. 

        :param id: Node identifier (UUIDv7) — the endpoint scope. (required)
        :type id: UUID
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path `id`; a credential belonging to a different Node surfaces as 403 `node_id_mismatch`.  (required)
        :type authorization: str
        :param endpoint_request: (required)
        :type endpoint_request: EndpointRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._put_node_endpoint_serialize(
            id=id,
            authorization=authorization,
            endpoint_request=endpoint_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "EndpointResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '410': "Problem",
            '413': "Problem",
            '501': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _put_node_endpoint_serialize(
        self,
        id,
        authorization,
        endpoint_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        # process the form parameters
        # process the body parameter
        if endpoint_request is not None:
            _body_params = endpoint_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='PUT',
            resource_path='/v1/nodes/{id}/endpoint',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    def put_node_state_report(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the report-write scope.")],
        key: Annotated[str, Field(strict=True, description="Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters). ")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may write a report; a missing, malformed, or revoked credential surfaces as 401. ")],
        node_state_report_request: NodeStateReportRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> NodeStateReportResponse:
        """Record an upstream node-state report entry for a Node.

        Records one upstream, workload-authored node-state report entry for the addressed Node under the addressed key. The entry is stored with `kind = report`; report entries are Node-authored telemetry and — unlike the platform-owned metadata/data entries — do NOT fan out a `node_state_updated` event.  The caller authenticates with its Node Secret Key (NSK) in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only write its own reports. The stored entry surfaces in the `reports` bucket of the `state`/`reports` blocks on the next `GET /v1/nodes/{id}/state` reconciliation pull.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`. 

        :param id: Node identifier (UUIDv7) — the report-write scope. (required)
        :type id: UUID
        :param key: Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).  (required)
        :type key: str
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may write a report; a missing, malformed, or revoked credential surfaces as 401.  (required)
        :type authorization: str
        :param node_state_report_request: (required)
        :type node_state_report_request: NodeStateReportRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._put_node_state_report_serialize(
            id=id,
            key=key,
            authorization=authorization,
            node_state_report_request=node_state_report_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "NodeStateReportResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '413': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def put_node_state_report_with_http_info(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the report-write scope.")],
        key: Annotated[str, Field(strict=True, description="Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters). ")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may write a report; a missing, malformed, or revoked credential surfaces as 401. ")],
        node_state_report_request: NodeStateReportRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[NodeStateReportResponse]:
        """Record an upstream node-state report entry for a Node.

        Records one upstream, workload-authored node-state report entry for the addressed Node under the addressed key. The entry is stored with `kind = report`; report entries are Node-authored telemetry and — unlike the platform-owned metadata/data entries — do NOT fan out a `node_state_updated` event.  The caller authenticates with its Node Secret Key (NSK) in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only write its own reports. The stored entry surfaces in the `reports` bucket of the `state`/`reports` blocks on the next `GET /v1/nodes/{id}/state` reconciliation pull.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`. 

        :param id: Node identifier (UUIDv7) — the report-write scope. (required)
        :type id: UUID
        :param key: Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).  (required)
        :type key: str
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may write a report; a missing, malformed, or revoked credential surfaces as 401.  (required)
        :type authorization: str
        :param node_state_report_request: (required)
        :type node_state_report_request: NodeStateReportRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._put_node_state_report_serialize(
            id=id,
            key=key,
            authorization=authorization,
            node_state_report_request=node_state_report_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "NodeStateReportResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '413': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def put_node_state_report_without_preload_content(
        self,
        id: Annotated[UUID, Field(description="Node identifier (UUIDv7) — the report-write scope.")],
        key: Annotated[str, Field(strict=True, description="Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters). ")],
        authorization: Annotated[StrictStr, Field(description="`Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may write a report; a missing, malformed, or revoked credential surfaces as 401. ")],
        node_state_report_request: NodeStateReportRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Record an upstream node-state report entry for a Node.

        Records one upstream, workload-authored node-state report entry for the addressed Node under the addressed key. The entry is stored with `kind = report`; report entries are Node-authored telemetry and — unlike the platform-owned metadata/data entries — do NOT fan out a `node_state_updated` event.  The caller authenticates with its Node Secret Key (NSK) in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only write its own reports. The stored entry surfaces in the `reports` bucket of the `state`/`reports` blocks on the next `GET /v1/nodes/{id}/state` reconciliation pull.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`. 

        :param id: Node identifier (UUIDv7) — the report-write scope. (required)
        :type id: UUID
        :param key: Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).  (required)
        :type key: str
        :param authorization: `Bearer <NSK plaintext>` — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may write a report; a missing, malformed, or revoked credential surfaces as 401.  (required)
        :type authorization: str
        :param node_state_report_request: (required)
        :type node_state_report_request: NodeStateReportRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._put_node_state_report_serialize(
            id=id,
            key=key,
            authorization=authorization,
            node_state_report_request=node_state_report_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "NodeStateReportResponse",
            '400': "Problem",
            '401': "Problem",
            '403': "PermissionDenied",
            '404': "Problem",
            '413': "Problem",
            '501': "Problem",
            '500': "Problem",
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _put_node_state_report_serialize(
        self,
        id,
        key,
        authorization,
        node_state_report_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if id is not None:
            _path_params['id'] = id
        if key is not None:
            _path_params['key'] = key
        # process the query parameters
        # process the header parameters
        if authorization is not None:
            _header_params['Authorization'] = authorization
        # process the form parameters
        # process the body parameter
        if node_state_report_request is not None:
            _body_params = node_state_report_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json', 
                    'application/problem+json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='PUT',
            resource_path='/v1/nodes/{id}/state/reports/{key}',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )


