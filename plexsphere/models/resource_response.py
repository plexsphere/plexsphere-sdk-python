# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from plexsphere.models.resource_origin import ResourceOrigin
from plexsphere.models.resource_provisioning import ResourceProvisioning
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ResourceResponse(BaseModel):
    """
    Hydrated projection of a Tenancy Resource. The shape is shared by `CreateResource`, `GetResource`, and `ListProjectResources` — every read surface returns the same wire bytes for the same Resource so clients only need one binding. The `provisioning` sub-object is present only on `Origin=Provisioned` Resources. 
    """ # noqa: E501
    id: UUID = Field(description="Resource identifier (UUIDv7).")
    project_id: UUID = Field(description="Owning Project (UUIDv7) — exactly-one-parent rule. ")
    domain_id: UUID = Field(description="Owning Domain (UUIDv7). Denormalised from the parent Project so cross-Domain checks do not have to reload the Project on every Resource read. ")
    kind: Annotated[str, Field(min_length=1, strict=True, max_length=64)] = Field(description="Resource kind discriminator.")
    external_ref: Optional[StrictStr] = Field(default=None, description="Optional external-system reference. `null` when the Resource declared none. ")
    origin: ResourceOrigin
    created_at: datetime = Field(description="Resource creation timestamp (UTC).")
    updated_at: datetime = Field(description="Last-modified timestamp (UTC).")
    provisioning: Optional[ResourceProvisioning] = Field(default=None, description="Broker provisioning state. Present only on `Origin=Provisioned` Resources; omitted on `Origin=Adopted` Resources. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["id", "project_id", "domain_id", "kind", "external_ref", "origin", "created_at", "updated_at", "provisioning"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResourceResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of provisioning
        if self.provisioning:
            _dict['provisioning'] = self.provisioning.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResourceResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "project_id": obj.get("project_id"),
            "domain_id": obj.get("domain_id"),
            "kind": obj.get("kind"),
            "external_ref": obj.get("external_ref"),
            "origin": obj.get("origin"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at"),
            "provisioning": ResourceProvisioning.from_dict(obj["provisioning"]) if obj.get("provisioning") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


