# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PermissionDenied(BaseModel):
    """
    Specialised RFC 9457 problem-details body returned on 403 from the ReBAC authorizer. Extends `Problem` with a machine-readable denial reason, the traversed relation path, and the correlation id that pairs the response with the matching audit entry emitted by `internal/audit`.  DECISION: the schema is declared as a standalone object rather than via `allOf` against `Problem`. kin-openapi's validator and oapi-codegen both accept `allOf`, but the downstream TypeScript client we generate treats `allOf` members as intersections that collide on optional fields — duplicating the Problem fields in place keeps the generated Go and TS shapes identical and avoids the collision. 
    """ # noqa: E501
    type: StrictStr = Field(description="URI reference that identifies the problem type.")
    title: StrictStr = Field(description="Short, human-readable summary of the problem.")
    status: StrictInt = Field(description="HTTP status code generated for this occurrence (always 403).")
    detail: StrictStr = Field(description="Human-readable explanation for this occurrence.")
    instance: StrictStr = Field(description="URI reference identifying the specific occurrence.")
    code: Optional[StrictStr] = Field(default=None, description="Optional machine-readable error code (shared with `Problem`). ")
    reason: StrictStr = Field(description="Machine-readable denial reason. The enum values match `internal/audit.Reason.String` exactly: `out_of_scope` — the request carried no relevant subject binding for the object; `insufficient_relation` — a subject binding exists but no permission derivation grants the attempted action; `caveat_violation` — the tuple graph permitted the action but a CEL caveat (within_time_window, from_cidr, requires_assurance) evaluated to false. ")
    relation_path: Optional[List[StrictStr]] = Field(default=None, description="Ordered sequence of relations the authorizer traversed while evaluating the Check. Empty when the denial was decided before any relation lookup (typically `out_of_scope`). ")
    correlation_id: StrictStr = Field(description="Correlation id that pairs this denial with the matching audit entry emitted by `internal/audit`. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["type", "title", "status", "detail", "instance", "code", "reason", "relation_path", "correlation_id"]

    @field_validator('reason')
    def reason_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['out_of_scope', 'insufficient_relation', 'caveat_violation']):
            raise ValueError("must be one of enum values ('out_of_scope', 'insufficient_relation', 'caveat_violation')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PermissionDenied from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PermissionDenied from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "title": obj.get("title"),
            "status": obj.get("status"),
            "detail": obj.get("detail"),
            "instance": obj.get("instance"),
            "code": obj.get("code"),
            "reason": obj.get("reason"),
            "relation_path": obj.get("relation_path"),
            "correlation_id": obj.get("correlation_id")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


