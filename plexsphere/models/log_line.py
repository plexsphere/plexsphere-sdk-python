# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class LogLine(BaseModel):
    """
    A single structured log line in a `POST /v1/nodes/{id}/logs` batch. The batch body is newline-delimited JSON (`application/x-ndjson`), one `LogLine` object per line. Like every ingest record the line is tagged server-side with the originating Node's Domain and Project and routed to Grafana Loki; the platform never reads an identity subject or email off the line. `severity`, `message`, and `timestamp` are required; `unit` and `hostname` are optional. 
    """ # noqa: E501
    severity: StrictStr = Field(description="Syslog severity keyword of the log line. A value outside the closed set is rejected with `400 ingest_batch_malformed`. ")
    unit: Optional[StrictStr] = Field(default=None, description="Optional originating unit (the systemd unit or file-glob source) the line was collected from. ")
    hostname: Optional[StrictStr] = Field(default=None, description="Optional hostname the line was emitted on. ")
    message: StrictStr = Field(description="The log message body. Required and non-empty. PII that a line inadvertently carries is the customer's ingest-side responsibility; an optional per-Domain redaction filter can be enabled at ingest. ")
    timestamp: datetime = Field(description="RFC 3339 timestamp at which the line was emitted. Required. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["severity", "unit", "hostname", "message", "timestamp"]

    @field_validator('severity')
    def severity_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['emerg', 'alert', 'crit', 'err', 'warning', 'notice', 'info', 'debug']):
            raise ValueError("must be one of enum values ('emerg', 'alert', 'crit', 'err', 'warning', 'notice', 'info', 'debug')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of LogLine from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of LogLine from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "severity": obj.get("severity"),
            "unit": obj.get("unit"),
            "hostname": obj.get("hostname"),
            "message": obj.get("message"),
            "timestamp": obj.get("timestamp")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


