# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class BlueprintVersionCreateRequest(BaseModel):
    """
    Body for `POST /v1/blueprints/{id}/versions`. The service validates every field BEFORE persisting: the `provider_kinds` enum members, the `injection_strategy` enum, the `parameter_schema` document, and the structural XRD/Composition manifest pair.  The `parameter_schema` here is the canonical `{\"parameters\":[...]}` document the Blueprint Catalog stores — NOT the flattened array the `BlueprintVersionResponse` read surface projects. Authorship and read shapes differ on purpose: authors submit the document the aggregate parses, readers receive the projected parameter list. 
    """ # noqa: E501
    version: Annotated[str, Field(min_length=1, strict=True)] = Field(description="Version identifier, unique within the parent Blueprint.")
    xrd: Dict[str, Any] = Field(description="Crossplane CompositeResourceDefinition manifest as a JSON object. Validated structurally against the Composition before any write; a malformed pair surfaces as `400 invalid_manifest`. ")
    composition: Dict[str, Any] = Field(description="Crossplane Composition manifest as a JSON object. Its composite-type reference must name the composite type the XRD declares. ")
    parameter_schema: Dict[str, Any] = Field(description="Typed parameter-schema document of the form `{\"parameters\":[{\"name\":…,\"type\":…,\"required\":…,\"default\"?:…}]}`. A structurally invalid document surfaces as `400 invalid_parameter_schema`. ")
    provider_kinds: Annotated[List[StrictStr], Field(min_length=1)] = Field(description="Closed-set infrastructure substrates this version can target, one or more of `aws`, `gcp`, `hetzner`, `openstack`. The server validates each value through the domain provider-kind parser; an out-of-set value is rejected with `400 invalid_provider_kind`. Non-empty. ")
    injection_strategy: StrictStr = Field(description="Discriminator naming how this version threads request parameters into the rendered Composite Resource, one of `cloud-init-user-data`, `helm-values`, `provider-secret`. The server validates the value; an out-of-set value is rejected with `400 invalid_injection_strategy`. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["version", "xrd", "composition", "parameter_schema", "provider_kinds", "injection_strategy"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BlueprintVersionCreateRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BlueprintVersionCreateRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "version": obj.get("version"),
            "xrd": obj.get("xrd"),
            "composition": obj.get("composition"),
            "parameter_schema": obj.get("parameter_schema"),
            "provider_kinds": obj.get("provider_kinds"),
            "injection_strategy": obj.get("injection_strategy")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


