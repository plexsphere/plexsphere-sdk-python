# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from plexsphere.models.cloud_credential_status import CloudCredentialStatus
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CloudCredentialResponse(BaseModel):
    """
    Metadata-only projection of a Cloud Credential. The shape is shared by `GetCloudCredential`, `ListCloudCredentials`, and `RevokeCloudCredential` so clients only need one binding.  The projection deliberately omits the KV mount, KV path, KV version, and every byte of secret material — the storage location is a storage-internal detail the operator-facing surface has no reason to expose. 
    """ # noqa: E501
    id: UUID = Field(description="Cloud Credential identifier (UUIDv7).")
    cloud_id: UUID = Field(description="Identifier of the owning Cloud — the residency pivot the ReBAC gate authorises against. ")
    display_name: Annotated[str, Field(strict=True, max_length=256)] = Field(description="Human-readable Cloud Credential name.")
    version: Annotated[int, Field(strict=True, ge=1)] = Field(description="Monotonic broker-row version. Starts at 1 on issuance and increments on every rotate. ")
    status: CloudCredentialStatus
    expires_at: datetime = Field(description="Wall-clock expiry budget (UTC).")
    revoked_at: Optional[datetime] = Field(default=None, description="Revocation timestamp (UTC). `null` until the credential is revoked by an operator. ")
    expired_at: Optional[datetime] = Field(default=None, description="Expiry-observed timestamp (UTC). `null` until the sweeper marks the credential expired. ")
    created_at: datetime = Field(description="Aggregate creation timestamp (UTC).")
    updated_at: datetime = Field(description="Last-modified timestamp (UTC). Bumped by every lifecycle mutator — rotate, revoke, expire. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["id", "cloud_id", "display_name", "version", "status", "expires_at", "revoked_at", "expired_at", "created_at", "updated_at"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CloudCredentialResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CloudCredentialResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "cloud_id": obj.get("cloud_id"),
            "display_name": obj.get("display_name"),
            "version": obj.get("version"),
            "status": obj.get("status"),
            "expires_at": obj.get("expires_at"),
            "revoked_at": obj.get("revoked_at"),
            "expired_at": obj.get("expired_at"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


