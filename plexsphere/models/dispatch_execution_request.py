# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from plexsphere.models.action_kind import ActionKind
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class DispatchExecutionRequest(BaseModel):
    """
    Body for `POST /v1/projects/{project_id}/executions:dispatch`. It names the action to dispatch, the parameters to pass, and the target set. The target is EXACTLY ONE of a single `node_id` or an opaque label `selector` — supplying both, or neither, is rejected. 
    """ # noqa: E501
    action: StrictStr = Field(description="Name of the action to dispatch.")
    type: Optional[ActionKind] = Field(default=None, description="Kind of action to dispatch (`builtin` or `hook`). Defaults to `builtin` when omitted. ")
    parameters: Optional[Dict[str, Any]] = Field(default=None, description="Opaque JSON parameter document passed verbatim to the action. ")
    timeout_seconds: Optional[StrictInt] = Field(default=None, description="Per-dispatch execution timeout in whole seconds. A background reconciler times out an invocation whose Node never reports a terminal result within this window. ")
    selector: Optional[StrictStr] = Field(default=None, description="Opaque label selector resolving the target cohort. Supply EXACTLY ONE of `selector` or `node_id`; supplying both, or neither, is rejected. ")
    node_id: Optional[UUID] = Field(default=None, description="Identifier of a single target Node (UUIDv7). Supply EXACTLY ONE of `node_id` or `selector`; supplying both, or neither, is rejected. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["action", "type", "parameters", "timeout_seconds", "selector", "node_id"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DispatchExecutionRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DispatchExecutionRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "action": obj.get("action"),
            "type": obj.get("type"),
            "parameters": obj.get("parameters"),
            "timeout_seconds": obj.get("timeout_seconds"),
            "selector": obj.get("selector"),
            "node_id": obj.get("node_id")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


