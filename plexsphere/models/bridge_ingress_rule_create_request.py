# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class BridgeIngressRuleCreateRequest(BaseModel):
    """
    Body for `CreateBridgeIngressRule`. Creates one public-ingress rule keyed by `(resource_id, slug)`, with an additional `(resource_id, sni_host)` uniqueness constraint. 
    """ # noqa: E501
    slug: Annotated[str, Field(strict=True, max_length=63)] = Field(description="Stable identity of the rule within its Resource. Unique per `(resource_id, slug)`; a collision surfaces as `409 slug_conflict`. ")
    sni_host: Annotated[str, Field(min_length=1, strict=True)] = Field(description="TLS SNI host the rule terminates. Unique per `(resource_id, sni_host)`; a collision surfaces as `409 slug_conflict`. ")
    target_node_id: UUID = Field(description="Node the rule forwards to. A Node outside the bridge Resource's owning Domain surfaces as `400 target_node_not_in_domain`. ")
    target_port: Annotated[int, Field(le=65535, strict=True, ge=1)] = Field(description="TCP port on the target Node. Outside `1..65535` the write is rejected with `400 relay_port_out_of_range`. ")
    acme_account_ref: Optional[StrictStr] = Field(default=None, description="Optional opaque reference to the ACME account used to issue the rule's certificate. `null` or absent means the operator supplies certificates out of band rather than via ACME issuance. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["slug", "sni_host", "target_node_id", "target_port", "acme_account_ref"]

    @field_validator('slug')
    def slug_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[a-z0-9]+(-[a-z0-9]+)*$", value):
            raise ValueError(r"must validate the regular expression /^[a-z0-9]+(-[a-z0-9]+)*$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BridgeIngressRuleCreateRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BridgeIngressRuleCreateRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "slug": obj.get("slug"),
            "sni_host": obj.get("sni_host"),
            "target_node_id": obj.get("target_node_id"),
            "target_port": obj.get("target_port"),
            "acme_account_ref": obj.get("acme_account_ref")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


