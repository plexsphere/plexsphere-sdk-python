# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from plexsphere.models.revoke_reason import RevokeReason
from plexsphere.models.session_kind import SessionKind
from plexsphere.models.session_status import SessionStatus
from plexsphere.models.session_target import SessionTarget
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Session(BaseModel):
    """
    Metadata-only projection of a mediated Session. The shape is shared by `IssueSession` (nested in the issue response), `ListSessions`, and `GetSession`. It carries the session identity, the owning Project / Domain / Resource / Identity, the aggregate status, the issuance / expiry / last-active timestamps, and the per-kind target — but NEVER the signed token. The token is delivered exactly once, in the issue response, and never re-exposed through this projection. 
    """ # noqa: E501
    id: UUID = Field(description="Session identifier (UUIDv7). Equals the issued token's `jti`. ")
    project_id: UUID = Field(description="Identifier of the owning Project (UUIDv7).")
    domain_id: UUID = Field(description="Identifier of the owning Domain (UUIDv7) — the residency pivot the per-Domain session policy and concurrency budgets count against. ")
    resource_id: UUID = Field(description="Identifier of the targeted Resource (UUIDv7).")
    identity_id: UUID = Field(description="Identifier of the Identity the Session was issued to.")
    kind: SessionKind
    status: SessionStatus = Field(description="Aggregate lifecycle status of the Session.")
    issued_at: datetime = Field(description="Issuance timestamp (UTC).")
    expires_at: datetime = Field(description="Expiry timestamp (UTC) — issuance time plus the clamped TTL. ")
    last_active_at: datetime = Field(description="Timestamp of the most recent recorded activity (UTC); drives the idle-timeout sweeper. ")
    idle_timeout_seconds: Optional[StrictInt] = Field(default=None, description="Idle window in whole seconds — the Session is reclaimed when `last_active_at + idle_timeout` passes. ")
    revoked_at: Optional[datetime] = Field(default=None, description="Revocation timestamp (UTC); `null` while the Session is live. ")
    revoke_reason: Optional[RevokeReason] = Field(default=None, description="Reason the Session was revoked. Omitted while the Session is live. ")
    target: SessionTarget
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["id", "project_id", "domain_id", "resource_id", "identity_id", "kind", "status", "issued_at", "expires_at", "last_active_at", "idle_timeout_seconds", "revoked_at", "revoke_reason", "target"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Session from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of target
        if self.target:
            _dict['target'] = self.target.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Session from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "project_id": obj.get("project_id"),
            "domain_id": obj.get("domain_id"),
            "resource_id": obj.get("resource_id"),
            "identity_id": obj.get("identity_id"),
            "kind": obj.get("kind"),
            "status": obj.get("status"),
            "issued_at": obj.get("issued_at"),
            "expires_at": obj.get("expires_at"),
            "last_active_at": obj.get("last_active_at"),
            "idle_timeout_seconds": obj.get("idle_timeout_seconds"),
            "revoked_at": obj.get("revoked_at"),
            "revoke_reason": obj.get("revoke_reason"),
            "target": SessionTarget.from_dict(obj["target"]) if obj.get("target") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


