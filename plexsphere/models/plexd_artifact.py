# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from plexsphere.models.plexd_artifact_arch import PlexdArtifactArch
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PlexdArtifact(BaseModel):
    """
    One indexed plexd release line returned by `GetPlexdArtifact`. It carries the release `version`, every per-architecture build, the lifecycle `support_status`, and the `verdict` the supply-chain verification gate pronounced. The verbatim Sigstore bundle attesting each build is served separately by the `.sigstore` companion endpoint rather than inlined here. 
    """ # noqa: E501
    version: Annotated[str, Field(min_length=1, strict=True)] = Field(description="Upstream plexd release tag the registry indexes (for example a semver tag such as `v1.4.2`). ")
    support_status: StrictStr = Field(description="Closed-set lifecycle status of the release line. `supported` is a fully supported, eligible upgrade target; `deprecated` still verifies and is served but operators should migrate off it; `withdrawn` is no longer a valid upgrade target. The wire values are intentionally lowercase and map onto the domain's support-status literals. ")
    verdict: StrictStr = Field(description="Closed-set verification verdict the supply-chain gate pronounced on the release. `verified` means every artifact passed the Sigstore bundle and checksum verification gate; `unverified` marks a record that has not passed the gate and is never served as an eligible upgrade target. ")
    architectures: Annotated[List[PlexdArtifactArch], Field(min_length=1)] = Field(description="The per-architecture builds of this release. At least one build is always present, and no two builds share an architecture. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["version", "support_status", "verdict", "architectures"]

    @field_validator('support_status')
    def support_status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['supported', 'deprecated', 'withdrawn']):
            raise ValueError("must be one of enum values ('supported', 'deprecated', 'withdrawn')")
        return value

    @field_validator('verdict')
    def verdict_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['verified', 'unverified']):
            raise ValueError("must be one of enum values ('verified', 'unverified')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PlexdArtifact from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in architectures (list)
        _items = []
        if self.architectures:
            for _item_architectures in self.architectures:
                if _item_architectures:
                    _items.append(_item_architectures.to_dict())
            _dict['architectures'] = _items
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PlexdArtifact from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "version": obj.get("version"),
            "support_status": obj.get("support_status"),
            "verdict": obj.get("verdict"),
            "architectures": [PlexdArtifactArch.from_dict(_item) for _item in obj["architectures"]] if obj.get("architectures") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


