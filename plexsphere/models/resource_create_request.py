# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from plexsphere.models.resource_origin import ResourceOrigin
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ResourceCreateRequest(BaseModel):
    """
    Body for `POST /v1/projects/{project_id}/resources`. The `origin` field discriminates the two creation flows:    * `origin=adopted` requires `kind`; `external_ref` is     optional. The handler persists the Resource and returns     `201` with the hydrated `ResourceResponse`.   * `origin=provisioned` requires `kind`,     `blueprint_version_id`, and `cloud_credential_id`; the     optional `parameters` object carries the typed values     validated against the BlueprintVersion's     `parameter_schema`. The handler creates the Resource,     mints a Pending `ProvisionedResource` via the broker, and     returns `202` with a `Location` header.  A body with `origin` absent or outside `{adopted, provisioned}` is rejected with `400 invalid_resource_origin`. A body that mixes flow-specific fields with the wrong `origin` (e.g. a `blueprint_version_id` on `origin=adopted`) is rejected with `400 invalid_body`. 
    """ # noqa: E501
    origin: ResourceOrigin
    kind: Annotated[str, Field(min_length=1, strict=True, max_length=64)] = Field(description="Resource kind discriminator. Trimmed and bounded by the tenancy aggregate's `resourceKindMaxLen`. ")
    external_ref: Optional[Annotated[str, Field(min_length=1, strict=True, max_length=256)]] = Field(default=None, description="Optional external-system reference for the adopted flow (URN, ARN, vendor-opaque id). Trimmed; whitespace-only is rejected. Omit on the provisioned flow. ")
    blueprint_version_id: Optional[UUID] = Field(default=None, description="BlueprintVersion the broker should render. REQUIRED on `origin=provisioned`; omit on `origin=adopted`. A missing version row surfaces as `422` with `code: blueprint_not_found`. ")
    cloud_credential_id: Optional[UUID] = Field(default=None, description="Cloud Credential the broker uses to provision the substrate. REQUIRED on `origin=provisioned`; omit on `origin=adopted`. A missing credential row surfaces as `422` with `code: cloud_credential_not_found`. ")
    parameters: Optional[Dict[str, Any]] = Field(default=None, description="Typed parameter values for `origin=provisioned`, validated against the BlueprintVersion's typed `parameter_schema`. Omit on `origin=adopted`. The wire envelope is opaque here; type-shape rejections surface at the handler. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["origin", "kind", "external_ref", "blueprint_version_id", "cloud_credential_id", "parameters"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResourceCreateRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResourceCreateRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "origin": obj.get("origin"),
            "kind": obj.get("kind"),
            "external_ref": obj.get("external_ref"),
            "blueprint_version_id": obj.get("blueprint_version_id"),
            "cloud_credential_id": obj.get("cloud_credential_id"),
            "parameters": obj.get("parameters")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


