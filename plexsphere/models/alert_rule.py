# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt
from typing import Any, ClassVar, Dict, List, Union
from typing_extensions import Annotated
from uuid import UUID
from plexsphere.models.alert_comparator import AlertComparator
from plexsphere.models.alert_severity import AlertSeverity
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AlertRule(BaseModel):
    """
    A stored alert rule for a Domain. Alert rules are stored, managed configuration only: the platform records and serves them and does not evaluate them in this phase. 
    """ # noqa: E501
    id: UUID = Field(description="Stable identifier of the alert rule (UUIDv7).")
    domain_id: UUID = Field(description="Domain the alert rule is scoped to.")
    name: Annotated[str, Field(strict=True, max_length=200)] = Field(description="Domain-unique logical name the rule is addressed by. ")
    signal: Annotated[str, Field(strict=True, max_length=1024)] = Field(description="The metric or log series expression the rule records as its watched signal. ")
    comparator: AlertComparator
    threshold: Union[StrictFloat, StrictInt] = Field(description="Finite value the signal is recorded against. NaN and infinity are rejected at creation. ")
    severity: AlertSeverity
    enabled: StrictBool = Field(description="Whether the rule is recorded as enabled. The flag is stored configuration only and does not cause the platform to evaluate the rule in this phase. ")
    created_at: datetime = Field(description="RFC 3339 timestamp the rule was first stored.")
    updated_at: datetime = Field(description="RFC 3339 timestamp the rule was last updated.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["id", "domain_id", "name", "signal", "comparator", "threshold", "severity", "enabled", "created_at", "updated_at"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AlertRule from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AlertRule from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "domain_id": obj.get("domain_id"),
            "name": obj.get("name"),
            "signal": obj.get("signal"),
            "comparator": obj.get("comparator"),
            "threshold": obj.get("threshold"),
            "severity": obj.get("severity"),
            "enabled": obj.get("enabled"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


