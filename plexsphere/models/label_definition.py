# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from plexsphere.models.label_value_schema import LabelValueSchema
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class LabelDefinition(BaseModel):
    """
    Response shape echoing a persisted Label Definition aggregate . 
    """ # noqa: E501
    id: UUID = Field(description="Label Definition identifier (UUIDv7).")
    scope: StrictStr = Field(description="Scope discriminator. Platform Definitions are visible to every tenant; Domain and Project scope restrict the visibility to the respective aggregate. ")
    scope_id: UUID = Field(description="Tenancy identifier. Zero UUID when `scope=platform`; non-zero Domain identifier when `scope=domain`; non-zero Project identifier when `scope=project`. ")
    local_key: StrictStr = Field(description="Unqualified key token (for example `env`, `cost-center`). ")
    qualified_key: StrictStr = Field(description="Fully namespaced key (for example `platform/env`, `acme:checkout/owner`). Unique across the platform. ")
    value_schema: LabelValueSchema
    applicable_kinds: List[StrictStr] = Field(description="Lowercase object-kind whitelist (resource, node, project, domain, workload, network, …). Assignments whose `object.kind` is not in this set are rejected with `scope-mismatch`. ")
    on_delete: StrictStr = Field(description="Policy applied when the Definition is deleted while Assignments still exist. ")
    cardinality: StrictInt = Field(description="Declared per-object cardinality cap for Assignments of this Definition. `1` means at most one Assignment per object; `0` means unlimited (bounded only by the global 64-per-object ceiling). ")
    immutable: StrictBool = Field(description="When `true`, the value schema is frozen and Assignments of this Definition may not have their value replaced . ")
    cloud_tag_propagation: StrictBool = Field(description="When `true`, matching cloud tags are synchronised onto cloud resources by the Provisioning Broker. ")
    description: Optional[StrictStr] = Field(default=None, description="Optional human description (at most 512 chars).")
    created_by: StrictStr = Field(description="Provenance of the aggregate. `system` is reserved for seed Definitions owned by the platform itself. ")
    created_at: datetime
    updated_at: datetime
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["id", "scope", "scope_id", "local_key", "qualified_key", "value_schema", "applicable_kinds", "on_delete", "cardinality", "immutable", "cloud_tag_propagation", "description", "created_by", "created_at", "updated_at"]

    @field_validator('scope')
    def scope_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['platform', 'domain', 'project']):
            raise ValueError("must be one of enum values ('platform', 'domain', 'project')")
        return value

    @field_validator('on_delete')
    def on_delete_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['block', 'cascade', 'orphan']):
            raise ValueError("must be one of enum values ('block', 'cascade', 'orphan')")
        return value

    @field_validator('created_by')
    def created_by_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['user', 'system']):
            raise ValueError("must be one of enum values ('user', 'system')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of LabelDefinition from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of value_schema
        if self.value_schema:
            _dict['value_schema'] = self.value_schema.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of LabelDefinition from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "scope": obj.get("scope"),
            "scope_id": obj.get("scope_id"),
            "local_key": obj.get("local_key"),
            "qualified_key": obj.get("qualified_key"),
            "value_schema": LabelValueSchema.from_dict(obj["value_schema"]) if obj.get("value_schema") is not None else None,
            "applicable_kinds": obj.get("applicable_kinds"),
            "on_delete": obj.get("on_delete"),
            "cardinality": obj.get("cardinality") if obj.get("cardinality") is not None else 1,
            "immutable": obj.get("immutable"),
            "cloud_tag_propagation": obj.get("cloud_tag_propagation"),
            "description": obj.get("description"),
            "created_by": obj.get("created_by"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


