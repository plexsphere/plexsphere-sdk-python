# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class MeshEdge(BaseModel):
    """
    Single directed edge in the per-Domain mesh-topology projection. Mirrors a SSE peer-delta row one-for-one — a renderer that consumes the live stream and a renderer that polls `GET /v1/domains/{domainId}/mesh/topology` converge on byte-identical state. 
    """ # noqa: E501
    source_node_id: UUID = Field(description="Node from which the WireGuard handshake originates. Pinned against the live Peer aggregate so a stale read never returns an edge for a retired Node. ")
    target_node_id: UUID = Field(description="Peer Node the handshake terminates against. Same liveness guarantee as `source_node_id`. ")
    mode: StrictStr = Field(description="`direct` when the edge is using a direct WireGuard handshake against the peer's NAT-observed endpoint; `relayed` when the bridge fallback endpoint assigned to this pair is currently in use. ")
    relay_node_id: UUID = Field(description="Bridge Node currently serving as the relay fallback for this edge. Always populated when `mode` is `relayed`; always `null` when `mode` is `direct`. ")
    status: StrictStr = Field(description="Derived edge health, computed by applying the per-Domain reachability policy thresholds to the per-edge handshake observation. The handshake telemetry itself is deliberately NOT exposed on this envelope — see the `MeshTopologyPage` handshake-telemetry follow-up anchor for the rationale and the planned schema extension. Uses the same enum values as the per-Node `Reachability` projection so a single renderer can colour both surfaces from one mapping. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["source_node_id", "target_node_id", "mode", "relay_node_id", "status"]

    @field_validator('mode')
    def mode_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['direct', 'relayed']):
            raise ValueError("must be one of enum values ('direct', 'relayed')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['healthy', 'stale', 'unreachable']):
            raise ValueError("must be one of enum values ('healthy', 'stale', 'unreachable')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MeshEdge from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MeshEdge from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "source_node_id": obj.get("source_node_id"),
            "target_node_id": obj.get("target_node_id"),
            "mode": obj.get("mode"),
            "relay_node_id": obj.get("relay_node_id"),
            "status": obj.get("status")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


