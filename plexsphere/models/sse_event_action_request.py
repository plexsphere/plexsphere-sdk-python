# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints, and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file.  CSRF defence-in-depth: every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf_token_mismatch`, `csrf_origin_mismatch`, `csrf_origin_not_configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from plexsphere.models.action_kind import ActionKind
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class SSEEventActionRequest(BaseModel):
    """
    Payload of the `action_request` Signed Event Bus event delivered to the target Node's event stream. The Node runs the dispatched action from this envelope and reports its terminal result back to the carried callback URL. One Execution fans out to one event per target Node, so two targets of the same dispatch share an `execution_id` but carry distinct `node_id` and `event_id`. 
    """ # noqa: E501
    event_id: UUID = Field(description="Fresh per-dispatch-event identifier (UUIDv7), distinct from `execution_id` so the event carries its own identity. ")
    occurred_at: datetime = Field(description="Timestamp the event was produced (UTC).")
    execution_id: UUID = Field(description="Identifier of the Execution the dispatch belongs to (UUIDv7).")
    node_id: UUID = Field(description="The single target Node this event addresses (UUIDv7).")
    action: StrictStr = Field(description="Name of the dispatched action.")
    type: ActionKind
    parameters: Optional[Dict[str, Any]] = Field(default=None, description="Opaque JSON parameter document passed verbatim to the action. `null` when the dispatch carries no parameters. ")
    timeout_seconds: Annotated[int, Field(strict=True, ge=0)] = Field(description="Per-dispatch timeout in whole seconds the Node must report a terminal result within. ")
    callback_url: StrictStr = Field(description="Absolute URL the Node reports its result back to, built per the template `{base}/v1/nodes/{node_id}/executions/{execution_id}`. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["event_id", "occurred_at", "execution_id", "node_id", "action", "type", "parameters", "timeout_seconds", "callback_url"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SSEEventActionRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SSEEventActionRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "event_id": obj.get("event_id"),
            "occurred_at": obj.get("occurred_at"),
            "execution_id": obj.get("execution_id"),
            "node_id": obj.get("node_id"),
            "action": obj.get("action"),
            "type": obj.get("type"),
            "parameters": obj.get("parameters"),
            "timeout_seconds": obj.get("timeout_seconds"),
            "callback_url": obj.get("callback_url")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


