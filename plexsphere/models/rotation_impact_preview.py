# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from uuid import UUID
from plexsphere.models.rotation_impact_edge import RotationImpactEdge
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RotationImpactPreview(BaseModel):
    """
    Body for GET /v1/nodes/{id}/keys/rotate/preview success responses. Carries a non-mutating dry-run of the fleet impact a mesh-key rotation against the addressed Node would have: the live Peer the rotation would target, the listing of peer edges whose PSK would be re-issued, whether a rotation is already in flight, and an estimated wall-clock duration so the dashboard confirmation dialog can size its progress indicator.  The response carries NO PSK plaintext and NO ciphertext, and the handler appends NO `peer_key_rotation` row, NO `rotate_keys` outbox event, and NO audit row beyond the read- side ReBAC decision — calling this surface twice in a row is guaranteed to be a no-op against the persistence layer. 
    """ # noqa: E501
    node_id: UUID = Field(description="The Node whose rotation is being previewed. Echoes the path parameter so a renderer can sanity-check the response against the request URL. ")
    peer_id: UUID = Field(description="Identifier of the live Peer the rotation would target. Mirrors `RotationTriggerResponse.peer_id` so the preview and the mutating trigger share a single mapping. ")
    affected_peer_count: Annotated[int, Field(strict=True, ge=0)] = Field(description="Number of peers whose pairwise PSK would be re-issued by the rotation. Equal to the length of `affected_edges`; duplicated as a top-level scalar so a renderer can show the count without expanding the edge listing. ")
    affected_edges: List[RotationImpactEdge] = Field(description="Explicit listing of peer edges that would receive a fresh pairwise PSK. Order is stable but unspecified — renderers MUST NOT rely on the listing order for layout. ")
    already_pending: StrictBool = Field(description="True when a `peer_key_rotation` row is already pending for the addressed Node — i.e. a live `POST /v1/nodes/{id}/keys/rotate` would surface the same flag and append no second event. Operators switch on this flag to distinguish a fresh trigger from an idempotent no-op before confirming the rotation. ")
    estimated_duration_seconds: Annotated[int, Field(strict=True, ge=0)] = Field(description="Estimated wall-clock seconds the rotation will take, based on the current SSE fan-out and the affected peer count. Capacity planning hint — not a guarantee. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["node_id", "peer_id", "affected_peer_count", "affected_edges", "already_pending", "estimated_duration_seconds"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RotationImpactPreview from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in affected_edges (list)
        _items = []
        if self.affected_edges:
            for _item_affected_edges in self.affected_edges:
                if _item_affected_edges:
                    _items.append(_item_affected_edges.to_dict())
            _dict['affected_edges'] = _items
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RotationImpactPreview from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "node_id": obj.get("node_id"),
            "peer_id": obj.get("peer_id"),
            "affected_peer_count": obj.get("affected_peer_count"),
            "affected_edges": [RotationImpactEdge.from_dict(_item) for _item in obj["affected_edges"]] if obj.get("affected_edges") is not None else None,
            "already_pending": obj.get("already_pending"),
            "estimated_duration_seconds": obj.get("estimated_duration_seconds")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


