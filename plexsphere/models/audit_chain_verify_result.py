# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AuditChainVerifyResult(BaseModel):
    """
    Outcome of a verify run. DIVERGENCE IS NOT AN ERROR — a tampered chain surfaces here as `ok: false` with the offending `divergent_seq` and the expected/observed hashes pinned. The handler MUST NEVER return a 5xx for tampering; the 5xx channel is reserved for infrastructure faults the verifier cannot reason about. 
    """ # noqa: E501
    ok: StrictBool = Field(description="`true` when every row in the inspected segment hashes correctly and chains to its predecessor; `false` when the verifier observed a divergence at `divergent_seq`. ")
    divergent_seq: Optional[StrictInt] = Field(default=None, description="Seq of the first row whose recomputed hash did not match the stored value, or whose `prev_hash` did not match the previous row's `entry_hash`. `null` on a clean run. ")
    expected_hash: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Hash the verifier computed for `divergent_seq`, lowercase hex. `null` on a clean run. ")
    observed_hash: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Hash actually stored at `divergent_seq`, lowercase hex. `null` on a clean run. ")
    segment_from: Annotated[int, Field(strict=True, ge=1)] = Field(description="Inclusive lower bound the verifier actually walked.")
    segment_to: Annotated[int, Field(strict=True, ge=1)] = Field(description="Inclusive upper bound the verifier actually walked.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["ok", "divergent_seq", "expected_hash", "observed_hash", "segment_from", "segment_to"]

    @field_validator('expected_hash')
    def expected_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9a-f]{64}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9a-f]{64}$/")
        return value

    @field_validator('observed_hash')
    def observed_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9a-f]{64}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9a-f]{64}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AuditChainVerifyResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AuditChainVerifyResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "ok": obj.get("ok"),
            "divergent_seq": obj.get("divergent_seq"),
            "expected_hash": obj.get("expected_hash"),
            "observed_hash": obj.get("observed_hash"),
            "segment_from": obj.get("segment_from"),
            "segment_to": obj.get("segment_to")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


