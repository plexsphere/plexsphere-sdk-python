# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PlexdHook(BaseModel):
    """
    Single discovered Kubernetes PlexdHook custom resource inside a CapabilityManifestRequest's `plexd_hooks` list. Distinct from `DeclaredHook` (the declared script hook): a PlexdHook carries an OCI image digest, a free-form parameter map, an execution timeout, and a sandbox flag rather than a payload checksum. Discovery is read-only on the plexsphere side — the manifest only records what plexd observed in the customer cluster; plexsphere never writes PlexdHook objects back nor resolves the digest against a registry. Per-entry invariants surface as 422 `plexd_hook_invalid`. 
    """ # noqa: E501
    name: Annotated[str, Field(min_length=1, strict=True)] = Field(description="PlexdHook resource name (e.g. `nightly-backup`). Non-empty after trimming whitespace; a violation surfaces as 422 `plexd_hook_invalid`. ")
    image_digest: StrictStr = Field(description="OCI image digest of the hook image in canonical `sha256:<64 lowercase hex>` form. Format-only validation — plexsphere performs no registry lookup. Anything that does not match surfaces as 422 `plexd_hook_invalid`. ")
    parameters: Optional[Dict[str, StrictStr]] = Field(default=None, description="Optional free-form string-to-string parameter map copied verbatim from the discovered PlexdHook spec. ")
    timeout_seconds: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Optional execution timeout in whole seconds. Non-negative; a negative value surfaces as 422 `plexd_hook_invalid`. The domain owns this as a duration — the `_seconds` suffix is the wire encoding only. ")
    sandbox: Optional[StrictBool] = Field(default=None, description="Optional flag indicating the discovered hook requests sandboxed execution. Absent is treated as `false`. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["name", "image_digest", "parameters", "timeout_seconds", "sandbox"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PlexdHook from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PlexdHook from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "image_digest": obj.get("image_digest"),
            "parameters": obj.get("parameters"),
            "timeout_seconds": obj.get("timeout_seconds"),
            "sandbox": obj.get("sandbox")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


