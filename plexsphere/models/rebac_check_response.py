# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RebacCheckResponse(BaseModel):
    """
    Result of `POST /v1/authz/check`. `decision` is the machine-readable outcome; `relation_path` is set only on `allowed`; `reason` is set only on `denied`. `correlation_id` is always set and pairs the response with the matching audit entry. 
    """ # noqa: E501
    decision: StrictStr = Field(description="ReBAC decision. `allowed` is the affirmative outcome; `denied` is a policy denial — NEVER an HTTP error. ")
    relation_path: Optional[List[StrictStr]] = Field(default=None, description="Ordered sequence of relations the authorizer traversed while evaluating the Check. Set only when `decision` is `allowed`. Empty array when the decision was reached without a relation lookup (typically a direct-binding allowance). ")
    reason: Optional[StrictStr] = Field(default=None, description="Machine-readable denial reason. Set only when `decision` is `denied`. Values match `internal/audit.Reason.String` extended with `granted` (allowance bookkeeping) and `unknown` (defensive default the authorizer never emits on a happy path). `out_of_scope`, `insufficient_relation`, and `caveat_violation` mirror the `PermissionDenied.reason` enum used by the 403 surface elsewhere in this spec. ")
    correlation_id: StrictStr = Field(description="Correlation id that pairs this decision with the matching audit entry emitted by `internal/audit`. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["decision", "relation_path", "reason", "correlation_id"]

    @field_validator('decision')
    def decision_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['allowed', 'denied']):
            raise ValueError("must be one of enum values ('allowed', 'denied')")
        return value

    @field_validator('reason')
    def reason_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['granted', 'out_of_scope', 'insufficient_relation', 'caveat_violation', 'unknown']):
            raise ValueError("must be one of enum values ('granted', 'out_of_scope', 'insufficient_relation', 'caveat_violation', 'unknown')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RebacCheckResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RebacCheckResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "decision": obj.get("decision"),
            "relation_path": obj.get("relation_path"),
            "reason": obj.get("reason"),
            "correlation_id": obj.get("correlation_id")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


