# coding: utf-8

"""
    plexsphere API

    HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/*`) is exempt because it cannot carry a Principal yet. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PlatformIdPBindingRequest(BaseModel):
    """
    Body for POST /v1/admin/platform-idp. Registers a platform-scoped (shared) IdP binding owned by no Domain and usable by any Domain. The field set mirrors IdPBindingRequest minus `domain_id` (a shared binding has no owning Domain) and `primary` (a shared binding has no Domain to be the default for). 
    """ # noqa: E501
    issuer: StrictStr = Field(description="OIDC issuer URL (absolute http/https).")
    client_id: StrictStr = Field(description="OIDC client identifier.")
    client_secret_ref: StrictStr = Field(description="Opaque reference to the client secret held in the secret store — never the secret value itself. ")
    discovery_url: StrictStr = Field(description="OIDC discovery document URL (absolute http/https).")
    claim_mappings: Optional[Dict[str, StrictStr]] = Field(default=None, description="Mapping of plexsphere claim name → IdP claim name. An empty map is permitted. The mappings apply uniformly to every Domain that resolves through this shared binding. ")
    required_acr: Optional[List[StrictStr]] = Field(default=None, description="Required OIDC ACR values.")
    required_amr: Optional[List[StrictStr]] = Field(default=None, description="Required OIDC AMR values.")
    jit_policy: StrictStr = Field(description="Just-in-time user-provisioning policy. One of `allow` or `deny`; the server rejects any other value with 400 `invalid-jit-policy`. Declared as a plain string rather than an inline enum so it shares the IdPBinding jit-policy vocabulary without minting a fourth generated enum type. ")
    alias: Optional[StrictStr] = Field(default=None, description="Optional human-friendly handle for the binding, unique among active platform bindings (e.g. `github`). Normalised to lowercase kebab-case, so an operator may submit mixed case. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["issuer", "client_id", "client_secret_ref", "discovery_url", "claim_mappings", "required_acr", "required_amr", "jit_policy", "alias"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PlatformIdPBindingRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PlatformIdPBindingRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "issuer": obj.get("issuer"),
            "client_id": obj.get("client_id"),
            "client_secret_ref": obj.get("client_secret_ref"),
            "discovery_url": obj.get("discovery_url"),
            "claim_mappings": obj.get("claim_mappings"),
            "required_acr": obj.get("required_acr"),
            "required_amr": obj.get("required_amr"),
            "jit_policy": obj.get("jit_policy"),
            "alias": obj.get("alias")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


